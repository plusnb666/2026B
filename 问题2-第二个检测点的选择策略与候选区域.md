# 问题2：第二个检测点的选择策略与候选区域

## 一、问题分析

已知某全向干扰源在第一个检测点 $S_1$ 处测得示向度 $\theta_1$。需要选择第二个检测点 $S_2$ 的位置，使得两个检测点交会定位获得的定位区域尽可能小（定位效果好），并给出 $S_2$ 的候选区域。

全向干扰源的信号在 $360^\circ$ 范围内均匀辐射，因此只要 $S_2$ 在干扰源的有效接收半径内，就能测得示向度。

定位效果的优劣由交会定位区域（两个角度扇区的交集）的大小衡量，区域越小定位越精确。定位区域的大小取决于两个因素：
1. **几何因素**：两条示向度线在干扰源处的交角 $\alpha$；
2. **距离因素**：两个检测点到干扰源的距离 $d_1, d_2$。

## 二、符号定义

| 符号 | 含义 |
|------|------|
| $S_1=(x_1,y_1)$ | 第一个检测点坐标（已知） |
| $\theta_1$ | 在 $S_1$ 处测得的示向度（已知） |
| $S_2=(x_2,y_2)$ | 第二个检测点坐标（待求） |
| $G$ | 干扰源真实位置（未知） |
| $d_1=\|S_1G\|$ | 第一个检测点到干扰源的距离（未知） |
| $d_2=\|S_2G\|$ | 第二个检测点到干扰源的距离 |
| $\alpha$ | 两条示向度线在干扰源 $G$ 处的交角，即 $\angle S_1GS_2$ |
| $\delta$ | 示向度误差半角，$\delta=1^\circ$ |
| $R$ | 干扰源有效接收半径，$R\in[1000,1500]$ 米 |
| $L_1$ | 从 $S_1$ 出发、方向为 $\theta_1$ 的示向度射线 |

## 三、定位精度与交角的关系

### 3.1 定位区域直径的近似公式

两个检测点的示向度扇区交集形成一个四边形（参见问题1）。设干扰源 $G$ 位于两条示向度中心线的交点附近，两条中心线在 $G$ 处的交角为 $\alpha$。

在 $G$ 附近，扇区 $W_1$ 的横向宽度约为 $2d_1\tan\delta$，扇区 $W_2$ 的横向宽度约为 $2d_2\tan\delta$。两个扇区交集形成的四边形可近似为平行四边形，其对角线（直径）满足：

$$\boxed{D \approx \frac{2\tan\delta \cdot \sqrt{d_1^2 + d_2^2}}{\sin\alpha}}$$

**推导**：平行四边形的两条邻边长度分别为 $a=2d_2\tan\delta/\sin\alpha$（沿 $L_1$ 方向）和 $b=2d_1\tan\delta/\sin\alpha$（沿 $L_2$ 方向），两边夹角为 $\alpha$。较长对角线长度为 $\sqrt{a^2+b^2+2ab\cos\alpha}$。当 $\alpha$ 不接近 $0^\circ$ 或 $180^\circ$ 时，可近似为 $\sqrt{a^2+b^2}=2\tan\delta\sqrt{d_1^2+d_2^2}/\sin\alpha$。在 $\alpha=90^\circ$ 时该近似精确成立。<span style="color:red">【修正】严格说 $D_{\mathrm{approx}}=\sqrt{a^2+b^2}$ 是真实直径（较长对角线 $\sqrt{a^2+b^2+2ab|\cos\alpha|}$）的**下界**：它略去了 $2ab|\cos\alpha|$ 项，$\alpha$ 越偏离 $90^\circ$ 低估越严重（§3.2 表最大约 32%）。且即使 $\alpha=90^\circ$，"精确"值 49.39 与近似值 49.37 仍有 0.04% 差，因"精确"列按楔形真实几何而非平行条带计算。故本公式用于定性判断最优值与趋势，定量直径以 §3.2 数值计算为准。</span>

### 3.2 数值验证

取 $d_1=d_2=1000$ 米，$\delta=1^\circ$，不同交角下的定位区域直径：

| 交角 $\alpha$ | 近似直径 (m) | 精确直径 (m) | 相对误差 |
|:---:|:---:|:---:|:---:|
| $10^\circ$ | 284.31 | 417.15 | 31.8% |
| $30^\circ$ | 98.74 | 135.46 | 27.1% |
| $60^\circ$ | 57.01 | 69.88 | 18.4% |
| $90^\circ$ | 49.37 | 49.39 | 0.0% |
| $120^\circ$ | 57.01 | 69.80 | 18.3% |
| $150^\circ$ | 98.74 | 134.84 | 26.8% |
| $170^\circ$ | 284.31 | 400.43 | 29.0% |

**结论**：
1. 定位区域直径在 $\alpha=90^\circ$ 时取得最小值，关于 $\alpha=90^\circ$ 对称；
2. 当 $\alpha$ 偏离 $90^\circ$ 时，直径按 $1/\sin\alpha$ 增大；
3. 近似公式在 $\alpha=90^\circ$ 时几乎精确，偏离 $90^\circ$ 时是真实直径的下界且低估增大，但趋势一致（真实直径关于 $\alpha=90^\circ$ 对称）。

<span style="color:red">【补充】表中"精确直径"列应由问题2数值脚本按楔形真实几何求交、再计算顶点对最大距离得到，当前数值需与代码输出核对一致（问题1已采用此做法，见 problem1_algorithm.py）。</span>

### 3.3 距离的影响

由近似公式 $D \propto \sqrt{d_1^2+d_2^2}$，在交角固定时，减小 $d_2$（第二个检测点到干扰源的距离）可以<span style="color:red">【修正】单调减小（亚线性，原"线性"不准确）</span>定位直径。但 $d_2$ 必须<span style="color:red">【修正】严格大于</span>5米，否则信号过强无法获得示向度（返回"near"）。<span style="color:red">【补充】题目规定"距离不超过 5 米"即返回 near，故 $d_2=5$ 米整仍在边界上，实际应取略大于 5 米。</span>

因此，**在保证 $\alpha\approx 90^\circ$ 的前提下，应使 $d_2$ 尽可能小（略大于5米）**。

## 四、第二个检测点的选择策略

### 4.1 最优策略（干扰源距离已知时）

若通过某种方式已知干扰源到 $S_1$ 的距离 $d_1$（例如根据信号强度估算，或问题3中的迭代定位），则最优的 $S_2$ 位置为：

$$\boxed{S_2^* = S_1 + d_1 \cdot (\cos\theta_1, \sin\theta_1) + \varepsilon \cdot (-\sin\theta_1, \cos\theta_1),\quad \varepsilon>5\,\text{米}}$$

即：沿示向度方向前进 $d_1$ 米到达干扰源附近，再垂直偏移 $\varepsilon$ 米（略大于5）。此时 $\alpha=90^\circ$，$d_2=\varepsilon$ 取最小可行值，定位直径最小。<span style="color:red">【修正】垂直偏移应取 $\varepsilon>5$ 米而非恰 5 米：$d_2=5$ 米整落在"距离不超过 5 米"的 near 边界上，无法获得示向度。下表按 $d_2=5$ 米作理想下界展示，实现取 $\varepsilon=6$ 米或略大。</span>

**数值示例**（$S_1=(0,0), \theta_1=0^\circ$）：

| $d_1$ (m) | 最优 $S_2$ 坐标 | 定位直径 (m) |
|:---:|:---:|:---:|
| 100 | (100, 5) | 3.50 |
| 500 | (500, 5) | 17.46 |
| 1000 | (1000, 5) | 34.92 |
| 1500 | (1500, 5) | 52.38 |

### 4.2 实用策略（干扰源距离未知时）

在问题2的设定下，干扰源的确切位置未知，只知道它在示向度射线 $L_1$ 上，且 $d_1\in(5, R]$，$R\in[1000,1500]$。

此时无法精确做到 $\alpha=90^\circ$ 对所有可能的 $G$ 都成立。策略是选择 $S_2$ 使得：

1. **对所有可能的干扰源位置 $G\in L_1$，交角满足 $\sin\alpha(G)\ge\sin\alpha_{\min}$（即 $\alpha(G)\in[\alpha_{\min},\,180^\circ-\alpha_{\min}]$）**<span style="color:red">【修正】原"交角不低于 $\alpha_{\min}$"不准确：直径 $\propto 1/\sin\alpha$，$\alpha$ 接近 $180^\circ$ 与接近 $0^\circ$ 同样差，故应约束 $\sin\alpha$（$\alpha$ 同时远离 $0^\circ$ 与 $180^\circ$），而非单侧 $\alpha\ge\alpha_{\min}$。</span>（保证定位精度不会太差）；
2. **$S_2$ 到所有可能的 $G$ 的距离不超过有效接收半径 $R$**（保证能检测到信号）；
3. **$S_2$ 到所有可能的 $G$ 的距离大于5米**（保证能获得示向度）。

在满足上述条件的候选区域内，优先选择使最坏情况下（即定位直径最大的 $G$）定位直径最小的 $S_2$。

通过数值搜索（$d_1\in[5,1500]$，$R=1500$），得到鲁棒最优位置约为 $S_2\approx(1108, 1000)$（局部坐标系，$S_1$ 在原点，示向度沿x轴），最坏情况下定位直径约80米。<span style="color:red">【补充】该点对应最小交角约 $\alpha_{\min}\approx48^\circ$（略大于 45°），与 §6.1 推荐 $\alpha_{\min}=45^\circ$ 的顶点 $(752.5,747.5)$（其最坏直径约 91 米）属不同准则。二者（min-max 最坏直径最优 vs 固定阈值+取解析点）应在论文中统一，且本数值须由配套脚本复现。</span>

## 五、候选区域的数学表示

### 5.1 局部坐标系

以 $S_1$ 为原点，示向度方向 $\theta_1$ 为 $x$ 轴正方向，建立局部坐标系。在此坐标系中：

- 干扰源 $G$ 位于 $x$ 轴上，坐标为 $(g, 0)$，其中 $g\in(g_{\min}, g_{\max}]$，$g_{\min}=5$，$g_{\max}=R$（有效接收半径，保守取1500米）；
- 第二个检测点 $S_2$ 的局部坐标为 $(x, y)$。

<span style="color:red">【补充】此处把干扰源当作严格位于示向度射线（中心线）上，作了一维简化。严格地，示向度有 $\pm1^\circ$ 误差，真实源在扇区 $[\theta_1-1^\circ,\theta_1+1^\circ]$ 内，横向偏移可达 $d_1\tan1^\circ\approx0.017\,d_1$（$d_1=1500$ 米时约 26 米）。该横向误差对候选区域形状的影响是二阶小量，本文作为简化假设忽略，仅保留沿射线方向的距离不确定性。</span>

### 5.2 交角约束

在局部坐标系中，干扰源 $G=(g,0)$ 处的交角为：

$$\alpha(g) = \arccos\left(\frac{g-x}{\sqrt{(g-x)^2+y^2}}\right)$$

<span style="color:red">【修正】要求 $\sin\alpha(g)\ge\sin\alpha_{\min}$（即 $\alpha(g)\in[\alpha_{\min},180^\circ-\alpha_{\min}]$）对所有 $g$ 成立。原"$\alpha(g)\ge\alpha_{\min}$"与下式不等价（如 $\alpha=170^\circ$ 满足 $\alpha\ge45^\circ$ 却不满足 $|\cos\alpha|\le\cos45^\circ$）。由 $\cos$ 在 $[0,\pi]$ 单调递减，$\sin\alpha(g)\ge\sin\alpha_{\min}\iff|\cos\alpha(g)|\le\cos\alpha_{\min}$，即：</span>

$$\left|\frac{g-x}{\sqrt{(g-x)^2+y^2}}\right| \le \cos\alpha_{\min}$$

化简得：

$$|g-x| \le |y|\cdot\cot\alpha_{\min}$$

对所有 $g\in[g_{\min},g_{\max}]$ 成立，等价于：

$$\boxed{\max\left(|g_{\min}-x|,\ |g_{\max}-x|\right) \le |y|\cdot\cot\alpha_{\min},\quad x\in[g_{\min},g_{\max}]}$$

### 5.3 可检测性约束（最大距离）

<span style="color:red">【修正】原"存在 $g$ 使 $|S_2G|\le R$"（点到线段的**最小**距离 $\le R$）不足以保证检测到真实干扰源：真实源在某一未知 $g^*$ 处，只有对**所有**可能 $g$ 都有 $|S_2G(g)|\le R$（到两端点的**最大**距离 $\le R$）才必然可检测。此与 §4.2 条件 2、§7 的验证一致。</span>

对 $x\in[g_{\min},g_{\max}]$，$|S_2G(g)|=\sqrt{(x-g)^2+y^2}$ 关于 $g$ 为凸函数，最大值在端点取得，故可检测性约束为：

$$\boxed{\sqrt{(x-g_{\min})^2+y^2}\le R\quad\text{且}\quad\sqrt{(x-g_{\max})^2+y^2}\le R}$$

即 $S_2$ 需同时落在以 $(g_{\min},0)$、$(g_{\max},0)$ 为圆心、半径 $R$ 的两个圆内（圆弧约束）。

### 5.4 距离不过近约束

对所有 $g\in[g_{\min},g_{\max}]$，$|S_2G|>5$（否则无法获得示向度）。等价于 $S_2$ 到线段 $[g_{\min},g_{\max}]$ 的最小距离大于5：

$$\boxed{\min_{g\in[g_{\min},g_{\max}]}\sqrt{(x-g)^2+y^2} > 5}$$

当 $x\in[g_{\min},g_{\max}]$ 时，最小距离为 $|y|$，故要求 $|y|>5$。

### 5.5 候选区域

综合以上约束，第二个检测点的候选区域为：

$$\boxed{
\Omega = \left\{(x,y)\ \middle|\ 
\begin{aligned}
& x\in[g_{\min},g_{\max}],\ |y|>5, \\
& \max(|g_{\min}-x|,|g_{\max}-x|) \le |y|\cot\alpha_{\min}, \\
& \sqrt{(x-g_{\min})^2+y^2}\le R,\quad \sqrt{(x-g_{\max})^2+y^2}\le R
\end{aligned}
\right\}
}$$

<span style="color:red">【修正】第三行可检测性由原"点到线段最小距离 $\le R$"改为"到两端点**最大**距离 $\le R$"（两个圆弧），见 §5.3。</span>

转换回全局坐标系：设局部坐标到全局坐标的旋转变换为 $T(x,y)=(x_1+x\cos\theta_1-y\sin\theta_1,\ y_1+x\sin\theta_1+y\cos\theta_1)$，则全局候选区域为 $T(\Omega)\cap\{x^2+y^2\le 1800^2\}$。

<span style="color:red">【补充】候选区域还须与目标圆域 $x^2+y^2\le1800^2$ 相交（干扰源分布区域为半径 1800 米的圆域，机器狗正常策略应在其内行动）。原文档遗漏该约束；当 $S_1$ 靠近区域边缘时，候选区域靠外一侧的顶点会落在圆域外。</span>

### 5.6 候选区域的几何形状

候选区域 $\Omega$ 关于 $x$ 轴对称，由上下两个曲边区域组成：

- **上半区域**：$y>0$，由直线 $y=(g_{\max}-x)\tan\alpha_{\min}$、$y=(x-g_{\min})\tan\alpha_{\min}$ 和可检测性圆弧围成；
- **下半区域**：$y<0$，与上半区域关于 $x$ 轴对称。

当 $\alpha_{\min}=45^\circ$ 时，$\cot\alpha_{\min}=1$，交角约束简化为 $|y|\ge\max(|g_{\min}-x|,|g_{\max}-x|)$，即 $y\ge g_{\max}-x$ 且 $y\ge x-g_{\min}$（上半区域）。<span style="color:red">【修正】原"形成一个以 $(g_{\max},0)$、$(g_{\min},0)$ 为底角、顶点在 $((g_{\min}+g_{\max})/2,(g_{\max}-g_{\min})/2)$ 的等腰三角形"描述反了：那个三角形（底在 $x$ 轴、顶点在上）满足 $y\le g_{\max}-x$ 且 $y\le x-g_{\min}$，是 V 形折线**下方**；候选区域是 V 形折线**上方**（$y\ge\cdots$），在施加可检测性上界前无界。</span>

候选区域的上边界由 §5.3 的两段圆弧给出：$y\le\sqrt{R^2-(x-g_{\min})^2}$ 且 $y\le\sqrt{R^2-(x-g_{\max})^2}$。记 $M(x)=\max(|x-g_{\min}|,|x-g_{\max}|)$，上半区域为

$$M(x)\tan\alpha_{\min}\le y\le\sqrt{R^2-M(x)^2},$$

仅在 $M(x)\le R\cos\alpha_{\min}$ 时非空。<span style="color:red">【补充】故候选区域非空的充要条件为 $R\cos\alpha_{\min}\ge(g_{\max}-g_{\min})/2$（$M(x)$ 在顶点 $x=(g_{\min}+g_{\max})/2$ 处取最小 $(g_{\max}-g_{\min})/2$）；当 $\alpha_{\min}=45^\circ$ 时要求 $R\ge(g_{\max}-g_{\min})/\sqrt2\approx1057$ 米。</span>

### 5.7 不同参数下的候选区域面积

取 $g_{\min}=5$，$g_{\max}=1500$（$(g_{\max}-g_{\min})/2=747.5$）。由对称性，总候选区域面积 $=4\int_{747.5}^{R\cos\alpha_{\min}}[\sqrt{R^2-m^2}-m\tan\alpha_{\min}]\,dm$：

<span style="color:red">【修正】下表按 §5.3 的**最大距离**（圆弧）可检测性重算，与原表（按最小距离）差异很大：原表在 $45^\circ,R=1000$ 给 $1.28\times10^5$ m²，实则应**为空**。</span>

| $\alpha_{\min}$ | $R=1000$ m | $R=1250$ m | $R=1500$ m |
|:---:|:---:|:---:|:---:|
| $30^\circ$ | $5.8\times10^4$ m² | $4.2\times10^5$ m² | $1.07\times10^6$ m² |
| $45^\circ$ | 0（空） | $7.1\times10^4$ m² | $3.6\times10^5$ m² |
| $60^\circ$ | 0（空） | 0（空） | $\approx0$（近乎空） |
| $75^\circ$ | 0（空） | 0（空） | 0（空） |

**分析**：
- 最小交角要求越高（$\alpha_{\min}$ 越大），候选区域越小；
- 候选区域非空当且仅当 $R\ge747.5\sec\alpha_{\min}$：$\alpha_{\min}=45^\circ$ 要求 $R\ge1057$ 米（故 $R=1000$ 为空），$\alpha_{\min}\ge60^\circ$ 基本不可行；
- <span style="color:red">【修正】故"建议取 $\alpha_{\min}=45^\circ$ 或 $60^\circ$"仅在 $R$ 接近 1500 时成立。由于 $R\in[1000,1500]$ 未知，稳妥做法是取 $\alpha_{\min}=30^\circ$（对所有 $R\ge1000$ 均非空）保证鲁棒可行性；或按 §5.6 的 $R\cos\alpha_{\min}\ge747.5$ 先判断当前 $R$ 下可用的 $\alpha_{\min}$ 上限。</span>

## 六、策略总结

### 6.1 选择策略

1. **建立局部坐标系**：以 $S_1$ 为原点，示向度方向 $\theta_1$ 为 $x$ 轴；
2. **确定参数**：取有效接收半径 $R=1500$（保守值）。最小交角阈值按 §5.7 取 $\alpha_{\min}=45^\circ$（仅当 $R\ge1057$ 可行）；若 $R$ 较小则按 $R\cos\alpha_{\min}\ge747.5$ 放宽（如 $\alpha_{\min}=30^\circ$ 对所有 $R\ge1000$ 均可行）；<span style="color:red">【修正】原"$45^\circ$ 或 $60^\circ$"未考虑可行性：$60^\circ$ 基本不可行。</span>
3. **计算候选区域**：按第5.5节公式计算 $\Omega$；
4. **选择最优位置**：在候选区域内，选择使最坏情况下定位直径最小的点。若计算资源有限，可直接取候选区域的"中心"位置：
   $$x^* = \frac{g_{\min}+g_{\max}}{2},\quad y^* = \frac{g_{\max}-g_{\min}}{2}\tan\alpha_{\min}$$
   （上半区域底部顶点。<span style="color:red">【修正】并非"对所有 $g$ 交角恰好为 $\alpha_{\min}$"：顶点处 $\alpha(g)$ 随 $g$ 变化，两端点取 $\alpha_{\min}$ 与 $180^\circ-\alpha_{\min}$、中点取 $90^\circ$，只是 $\sin\alpha(g)\ge\sin\alpha_{\min}$ 处处成立且两端点取等。该点是"到两端点最大距离最小"的鲁棒点，也非"中心"；真正的 min-max 最坏直径最优需数值搜索，如 §4.2 的 $(1108,1000)$（约 80 米）优于本顶点（约 91 米）。</span>）
5. **转换回全局坐标**：将 $(x^*, y^*)$ 旋转变换回全局坐标系。

### 6.2 候选区域

第二个检测点的候选区域是示向度射线 $L_1$ 两侧的对称带状区域，满足：
- 沿示向度方向的坐标 $x$ 在干扰源可能的距离范围 $[5, R]$ 内；
- 垂直偏移量 $|y|$ 足够大，保证对所有可能的干扰源位置 $\sin\alpha\ge\sin\alpha_{\min}$（即 $\alpha\in[\alpha_{\min},180^\circ-\alpha_{\min}]$）；
- 到示向度射线上任意可能干扰源位置的距离不超过有效接收半径 $R$，且大于5米。

推荐取 $\alpha_{\min}=45^\circ$，$R=1500$ 米，此时候选区域为两个对称的曲边三角（上下各一个），面积约 $3.6\times10^5$ 平方米。<span style="color:red">【修正】原"两个等腰三角形、面积 $1.14\times10^6$"按最小距离可检测性算得；按最大距离（圆弧）应为曲边三角、面积约 $3.6\times10^5$ m²，见 §5.7。</span>

## 七、数值示例

设 $S_1=(0,0)$，示向度 $\theta_1=0^\circ$（正东方向），取 $\alpha_{\min}=45^\circ$，$R=1500$ 米。

**候选区域**（局部坐标系 = 全局坐标系）：
- <span style="color:red">【修正】上半区域为曲边三角，关键顶点：$(439.3,\,1060.7)$、$(1065.7,\,1060.7)$（两侧：直线 $y=1500-x$、$y=x-5$ 分别与圆弧 $\sqrt{(x-1500)^2+y^2}=1500$、$\sqrt{(x-5)^2+y^2}=1500$ 的交点）、$(752.5,\,1300.5)$（顶部，两圆弧交点）、$(752.5,\,747.5)$（底部顶点）。原"$(5,1495),(1500,1495),(752.5,747.5)$"是按最小距离可检测性得到的错误顶点：$(5,1495)$ 到 $(1500,0)$ 的距离达 $\sqrt{1495^2+1495^2}=2114$ 米 $>1500$，实际不可检测。</span>
- 下半区域与上半区域关于 $x$ 轴对称。

**推荐选择点**：$S_2=(752.5, 747.5)$（候选区域底部顶点，到两端点最大距离最小）。

验证：对 $g=5$，$\alpha=135^\circ$，$\sin135^\circ=\sin45^\circ\ge\sin45^\circ$ ✓；
对 $g=1500$，$\alpha=45^\circ$，$\sin45^\circ\ge\sin45^\circ$ ✓；
对 $g=752.5$，$\alpha=90^\circ$，$\sin90^\circ=1$ ✓；
最大距离 $d_2=\max_g\sqrt{(g-752.5)^2+747.5^2}=\sqrt{747.5^2+747.5^2}=1057$ 米 $<1500$ ✓；
最小距离 $=\min_g\sqrt{(g-752.5)^2+747.5^2}=747.5>5$ ✓。
