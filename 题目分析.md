# B题：无线电干扰源的快速自动定位与清除

> 本节先用通俗语言解释题目。后面的内容再给出数学建模和算法方面的专业表述。

## 先用一句话说清楚这道题

有一片半径 1800 米的圆形区域，里面藏着 10 到 16 个无线电干扰源。机器狗从圆心出发，只能通过测向机一点点寻找它们，最后走到每个干扰源附近并把它清除掉。我们要设计一套尽可能快、尽可能不漏目标的行动方案。

## 题目背景：机器狗到底在做什么

可以把无线电干扰源想象成藏在地图上的“无线电小信标”。每个信标使用一个频道，频道互不重复。机器狗不知道信标在哪里，也不知道到底有多少个，更不知道某个信标的信号能传多远。

机器狗有三种关键能力：

1. **移动**：它可以在地图上直线移动，速度是每秒 5 米。
2. **测量**：在某个位置选择一个频道，花 5 秒判断有没有信号。如果有信号，还能得到“信号来自哪个方向”。
3. **清除**：机器狗走到干扰源 20 米以内，就可以用光学设备精确定位并清除它。成功清除一次需要 5 秒。

测量方向并不完全准确，误差最多约为 1 度。因此，机器狗得到的不是“目标就在这条线上的某一点”，而是“目标大概率在一条有宽度的方向带里”。从多个位置测量后，就可以把这些方向带重叠起来，逐渐缩小目标范围。

## 两种干扰源有什么区别

### 全向干扰源

全向干扰源像一盏向四面八方发光的灯。只要机器狗离它不太远，就能收到信号，不太受观察方向影响。

### 定向干扰源

定向干扰源像一盏只朝半边天空照射的灯。即使机器狗离它很近，只要站在它没有发射信号的一侧，也可能完全收不到信号。

所以，“没有检测到信号”并不一定说明这个频道没有干扰源，也可能只是机器狗站错了方向。这正是问题4比问题3困难很多的原因。

## 四个问题分别在问什么

### 问题1：已经测到几个方向，目标究竟可能在哪里？

题目给出若干检测点和每个点测到的方向。由于每次测量有误差，应该把每个方向画成一条窄窄的扇形带，而不是一条绝对准确的直线。

把所有扇形带重叠起来，剩下的区域就是干扰源可能出现的地方。问题1要求计算这个区域中相距最远的两点之间的距离，也就是定位区域的“最大宽度”，称为区域直径。

通俗地说，问题1是在问：**根据现有测量结果，我们最坏情况下还可能错多远？**

题目还问：以这个最大距离为直径画一个圆，能不能把整个定位区域装进去。不能想当然地认为一定可以，需要再检查定位区域的形状。

### 问题2：下一次应该站在哪里测，定位才会更准？

第一次测量只能知道目标大概在某个方向。第二次测量如果站在原地附近，或者仍然沿着同一个方向看，两条方向线几乎平行，交点会非常不稳定。

比较好的做法是换到侧面，从尽量接近直角的方向再看一次。这样两条方向带交叉得更明显，重叠区域更小。

通俗地说，问题2是在问：**机器狗下一步站在哪里，既不要走太远，又能从不同角度看目标？**

### 问题3：所有干扰源都是全向的，怎样把它们全部找出来？

这是一个“搜索路线设计”问题。机器狗需要：

1. 在区域内安排一些检测点；
2. 在这些点上检查不同频道；
3. 一旦收到某个频道的信号，就根据两个或多个方向估计目标位置；
4. 走到目标附近并清除；
5. 继续寻找其他目标，直到所有干扰源都被清除。

难点在于，机器狗不知道哪些频道真的有干扰源。一次没有信号，可能是频道不存在，也可能是距离太远。因此不能只测一次就放弃一个频道。

同时，机器狗每移动一次、切换一次频道、检测一次或清除一次，都会花时间。路线太保守会漏目标，路线太复杂又会超时。

通俗地说，问题3是在问：**怎样安排搜索顺序和行走路线，才能在有限时间内尽量找到并清除全部全向干扰源？**

### 问题4：有些干扰源是定向的，怎样避免漏掉它们？

问题4和问题3的区别只有一个，但影响很大：有些干扰源只向一个方向发射信号。

此时，在一个位置没有收到信号，不能说明附近没有目标。机器狗需要换到别的位置，甚至从多个方向绕着检查。如果某个频道长期没有信号，也不能马上删除它，而应该保留“它可能存在，只是暂时没有被照到”的状态。

通俗地说，问题4是在问：**当目标可能只朝某些方向发信号时，机器狗怎样主动换位置，减少漏检？**

## 这道题为什么难

### 1. 信息是不完整的

机器狗一开始不知道：

- 干扰源有多少个；
- 每个干扰源的位置；
- 干扰源使用哪个频道；
- 每个信号能传多远；
- 哪些干扰源是定向的；
- 定向干扰源朝哪个方向发射。

它只能根据每次测量结果逐步猜测。

### 2. “没有信号”有多种解释

收到信号通常说明附近存在目标，但没有信号并不能给出唯一结论。可能是目标不存在、目标已经被清除、距离太远，或者定向干扰源没有朝向机器狗。

### 3. 定位精度和行动时间互相矛盾

检测点越多，定位通常越准确，但机器狗需要移动和测量更多次；检测点太少，虽然省时间，却可能无法走到 20 米清除范围内。

### 4. 需要同时安排“搜哪里”和“先搜谁”

机器狗既要决定下一步去哪一个位置检测，也要决定先处理哪个频道、哪个已经定位的目标。这既像地图搜索，也像带时间成本的路线规划。

## 用一个简单例子理解定位

假设机器狗在位置 A 测到目标大概在东北方向，那么目标可能在从 A 出发的一条窄扇形区域里。

机器狗再走到位置 B，从西北方向测到目标。两条窄扇形区域相交的地方，就是目标最可能的位置。

如果两次观察方向接近直角，交叉区域通常比较小；如果两次观察方向几乎平行，交叉区域会很长，定位就不可靠。这就是问题2要求选择合适第二检测点的原因。

## 最后要优化的到底是什么

题目并不是单纯要求“找到一个目标”，而是要求在时间限制内尽可能完成全部任务。总时间包括：

- 机器狗移动的时间；
- 切换频道的时间；
- 每次检测的 5 秒；
- 光学精确定位和清除的时间；
- 走错位置后再次尝试的时间。

因此，一个好的方案要同时做到三件事：

1. **不漏掉目标**；
2. **定位足够准确，能够成功清除**；
3. **路线和频道顺序尽量高效**。

后文的专业模型，就是把这三件事转化为可以计算、编程和测试的规则。

## 一、题目概述

本题研究机器狗在二维目标区域内对无线电干扰源进行自动搜索、定位和清除。机器狗通过测向机获取干扰源的示向度，再结合移动、频道切换、交会定位和光学清除完成任务。

题目包含两类核心场景：

- 全向干扰源：在有效接收半径内均可检测到信号。
- 定向干扰源：除满足距离条件外，还必须位于干扰源的有效覆盖角度内才能检测到信号。

因此，本题是一个由几何定位、最优检测点设计、路径规划、组合优化和不完全信息在线决策组成的综合数学建模问题。

## 二、四个问题的类型

| 问题 | 主要类型 | 核心方法 |
| --- | --- | --- |
| 问题1 | 几何误差分析、计算几何 | 角度误差带、凸多边形、凸包、旋转卡壳 |
| 问题2 | 最优实验设计、检测点选址 | 交会角优化、误差传播、候选区域搜索 |
| 问题3 | 全向目标搜索、路径规划、组合优化 | 区域扫描、频道搜索、交会定位、旅行商问题 |
| 问题4 | 不完全信息下的在线搜索、鲁棒优化 | 多方向探测、候选区域更新、信息增益、滚动决策 |

本题的根本难点是：机器狗事先不知道干扰源数量、位置、频道、有效接收半径和定向方向，只能通过检测结果逐步推断环境。

---

# 三、问题1：交会定位区域及其直径

## 1. 角度误差带模型

设第 $i$ 个检测点为

$$
S_i=(x_i,y_i)
$$

测得的示向度为 $\theta_i$。由于测量误差位于 $[-1^\circ,1^\circ]$，真实干扰源位置 $G=(x,y)$ 必须满足

$$
\theta_i-1^\circ
\leq
\arg(G-S_i)
\leq
\theta_i+1^\circ
$$

因此，每个检测点对应一个角度带。多个检测点的交集就是定位区域：

$$
\Omega=\bigcap_{i=1}^{n}\Omega_i
$$

由于干扰源位于半径为 $1800$ 米的目标圆域内，还需要加入目标区域约束：

$$
\Omega_{\mathrm{real}}
=
\Omega\cap\{(x,y):x^2+y^2\leq1800^2\}
$$

## 2. 定位区域直径算法

定位区域直径定义为

$$
D=\max_{P,Q\in\Omega_{\mathrm{real}}}\|P-Q\|
$$

建议采用如下算法：

1. 根据每个检测点和示向度构造两条误差边界线；
2. 求任意两条边界线的交点；
3. 筛选满足所有角度约束和目标圆域约束的交点；
4. 对有效交点求凸包；
5. 在凸包上计算最远点对。

对于凸多边形，最远点对一定出现在顶点之间。若有效顶点为 $P_i=(x_i,y_i)$，也可以直接计算

$$
D=\max_{i,j}\sqrt{(x_i-x_j)^2+(y_i-y_j)^2}
$$

若要设计通用高效算法，可以使用“凸包 + 旋转卡壳”，时间复杂度约为 $O(n\log n)$。

## 3. 直径圆能否覆盖定位区域

不能仅凭定位区域直径作出肯定结论。

以 $D$ 为直径的圆半径为 $D/2$。应进一步求定位区域的最小覆盖圆，设其半径为 $R_{\min}$。判断条件为

$$
R_{\min}\leq\frac{D}{2}
$$

若成立，则该圆可以覆盖定位区域；否则不能。

对于细长区域，该圆通常可以覆盖；但对于接近等边三角形等形状，最小覆盖圆半径可能大于 $D/2$。

---

# 四、问题2：第二个检测点的选择策略

## 1. 交会角与定位误差

设两个检测点观测到的真实方向夹角为 $\alpha$。交会定位误差大致满足

$$
\text{定位误差}\propto\frac{1}{\sin\alpha}
$$

当 $\alpha$ 接近 $0^\circ$ 或 $180^\circ$ 时，两条方向线近似平行，定位误差很大；当

$$
\alpha\approx90^\circ
$$

时，定位效果最稳定。

## 2. 第二个检测点的选择原则

第二个检测点应满足：

- 与第一个检测点保持足够距离；
- 尽量位于第一条测向线的侧方；
- 使两次观测方向夹角接近 $90^\circ$；
- 尽量位于目标圆域内部；
- 对全向干扰源而言，尽量仍处于有效接收范围内；
- 同时考虑机器狗的移动距离。

直观地说，第一个检测点确定目标大致位于哪条射线上，第二个检测点应从侧面观察目标，避免沿原方向重复观测。

## 3. 候选区域

可以将第二检测点候选区域写为

$$
\mathcal S_2
=
\mathcal D_{1800}
\cap
\mathcal R_{\mathrm{可检测}}
\cap
\mathcal R_{\mathrm{良好交会}}
$$

其中：

- $\mathcal D_{1800}$ 是半径 $1800$ 米的目标圆域；
- $\mathcal R_{\mathrm{可检测}}$ 是能够接收到干扰源信号的区域；
- $\mathcal R_{\mathrm{良好交会}}$ 是使交会角处于合理范围，例如 $60^\circ\leq\alpha\leq120^\circ$ 的区域。

若同时考虑移动代价，可构造目标函数

$$
J(S_2)
=
 w_1\frac{1}{\sin\alpha(S_2)}
+w_2\cdot\text{移动距离}
+w_3\cdot\text{定位区域面积}
$$

选择使 $J(S_2)$ 最小的检测点。

因此，问题2本质上是一个带移动代价的最优实验设计问题，而不是简单地追求两个检测点距离最大。

---

# 五、问题3：全向干扰源的搜索与清除

## 1. 主要条件

问题3中：

- 所有干扰源均为全向干扰源；
- 干扰源数量为 $10$ 至 $16$ 个；
- 每个干扰源占用不同频道；
- 频道范围为 $1$ 至 $20$；
- 机器狗从 $(0,0)$ 出发，初始频道为 $1$；
- 机器狗移动速度为 $5$ 米/秒；
- 一次测量耗时 $5$ 秒；
- 任意频道切换耗时 $1$ 秒；
- 清除成功耗时 $5$ 秒，未发现目标耗时 $3$ 秒；
- 需要在时间限制内尽可能清除全部干扰源。

## 2. 主要困难

### 2.1 无法直接判断频道是否存在

一次 `no_signal` 可能表示：

- 该频道不存在干扰源；
- 该频道的干扰源已经被清除；
- 当前位置距离太远；
- 当前检测位置不满足接收条件。

因此，不能根据一次无信号结果直接判定频道不存在。

### 2.2 定位和路径规划相互影响

为了定位一个干扰源，需要多个检测点；但检测点越多，移动时间和测量时间越长。总时间应建模为

$$
T=T_{\mathrm{move}}+T_{\mathrm{channel}}+T_{\mathrm{measure}}+T_{\mathrm{clear}}
$$

目标是在保证尽可能多清除干扰源的前提下，使 $T$ 尽可能小。

### 2.3 频道切换具有时间代价

当前频道只会因 `/measure` 改变，而 `/clear` 不会改变当前频道。因此应尽量按频道顺序检测，并集中完成同一频道的测量。

## 3. 建议的搜索清除算法

### 阶段一：区域扫描

在半径 $1800$ 米的目标圆域内设置扫描点，可以采用：

- 同心圆扫描；
- 方格网扫描；
- 六边形网格扫描；
- 圆周与径向组合扫描。

在每个扫描点依次检测频道 $1$ 至 $20$，记录：

- 检测位置；
- 频道编号；
- 检测结果；
- 示向度；
- 当前频道状态。

### 阶段二：交会定位

对同一频道获得的多个 `direction` 结果，构造角度误差带并求交集，得到该干扰源的候选定位区域。

当定位区域直径足够小，例如小于 $40$ 米时，可进入清除阶段。

### 阶段三：选择清除位置

选择定位区域中心、质心或最小覆盖圆圆心作为清除点：

$$
P_{\mathrm{clear}}
=
\arg\min_P\max_{G\in\Omega}\|P-G\|
$$

若定位区域能够被以 $P_{\mathrm{clear}}$ 为圆心、半径 $20$ 米的圆覆盖，则执行 `/clear`。

清除成功后，将该频道标记为已清除；若失败，则增加检测点并重新定位。

### 阶段四：在线更新

每次检测和清除后更新频道状态：

| 状态 | 含义 |
| --- | --- |
| 未知 | 尚未获得足够信息 |
| 可能存在 | 曾经检测到信号 |
| 已定位 | 定位区域已经较小 |
| 已清除 | `/clear` 返回成功 |
| 暂无信号 | 当前点无信号，但不能判定不存在 |

## 4. 路径规划

可以建立节点图：

- 节点：扫描点、定位点、清除点；
- 边权：两点之间的移动时间；
- 额外代价：频道切换、测量和清除时间。

已定位多个目标后，可以使用最近邻算法，再用 2-opt 进行局部优化。也可以使用模拟退火、遗传算法或整数规划，但实际比赛中建议优先采用稳定、易调试的“固定扫描路线 + 最近邻清除 + 局部优化”方法。

---

# 六、问题4：含定向干扰源的搜索与清除

## 1. 定向干扰源造成的歧义

定向干扰源的有效覆盖角度为定向方向两侧各 $90^\circ$，总覆盖角度为

$$
180^\circ
$$

此时 `no_signal` 可能表示：

- 距离超过有效接收半径；
- 目标已被清除；
- 检测点不在有效覆盖扇区内；
- 该频道不存在干扰源。

因此，问题3中简单跳过无信号频道的策略，在问题4中可能漏检目标。

## 2. 多方向主动探测

对于疑似存在但暂时无信号的频道，应改变检测位置，而不是立即判定该频道不存在。

可以采用圆周绕行、螺旋搜索、径向进出或多角度环绕等方式。例如，以某个中心点为基准，在半径 $r$ 的圆周上设置检测点：

$$
S_k=S_0+r(\cos\phi_k,\sin\phi_k)
$$

其中

$$
\phi_k=0^\circ,45^\circ,90^\circ,\ldots,315^\circ
$$

通过改变检测方位，提高进入定向覆盖区域的概率。

## 3. 概率型频道状态

可以为每个频道维护一个存在概率

$$
p_j=P(\text{频道 }j\text{ 存在且尚未清除})
$$

根据检测结果进行更新：

- 检测到 `direction`：显著提高 $p_j$；
- 检测到 `near`：说明已经接近目标，应优先清除；
- 检测到 `no_signal`：降低 $p_j$，但不能直接置为零；
- 清除成功：令 $p_j=0$。

还可以维护每个频道的候选位置区域：

$$
\Omega_j^{(t)}
=
\{\text{第 }j\text{ 个频道干扰源的可能位置}\}
$$

每次检测根据结果缩小候选区域。

## 4. 信息增益检测策略

下一检测点的选择应同时考虑移动时间和预期信息增益。可以定义

$$
J(S)
=
\lambda_1\cdot\text{移动时间}
+\lambda_2\cdot\text{检测时间}
-\lambda_3\cdot\text{预期信息增益}
$$

其中信息增益可以用候选区域面积的期望减少量表示：

$$
IG(S)
=
\operatorname{Area}(\Omega_{\mathrm{before}})
-
\mathbb E[\operatorname{Area}(\Omega_{\mathrm{after}})]
$$

选择使 $J(S)$ 最小的检测点，即可在移动成本和探测收益之间取得平衡。这属于主动感知或主动学习问题。

## 5. 清除阶段的特殊性质

定向干扰源的检测受覆盖方向影响，但清除不受方向影响。只要清除位置满足

$$
\|P_{\mathrm{clear}}-G\|\leq20
$$

即可成功清除。

因此，问题4可以采用：

> 多方向搜索 + 角度误差带交会定位 + 光学精确清除。

---

# 七、必须严格处理的约束

## 1. 目标区域约束

$$
 x^2+y^2\leq1800^2
$$

机器狗可以向模拟器提交区域外坐标，但正常策略应尽量在目标区域内行动。

## 2. 移动时间

机器狗沿直线移动，速度为 $5$ 米/秒：

$$
 t_{\mathrm{move}}
 =
 \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{5}
$$

## 3. 检测时间

一次 `/measure` 的虚拟耗时为

$$
 t_{\mathrm{measure}}
 =
 t_{\mathrm{move}}
 +t_{\mathrm{channel}}
 +5
$$

其中频道切换时间为 $0$ 或 $1$ 秒。

## 4. 清除时间

一次 `/clear` 的虚拟耗时为

$$
 t_{\mathrm{clear}}
 =
 t_{\mathrm{move}}
 +
 \begin{cases}
 5,&\text{成功清除};\\
 3,&\text{未发现目标}.
 \end{cases}
$$

## 5. 接口与运行约束

- 一次只能发送一个请求，收到完整响应后才能发送下一请求；
- `/measure` 会改变测向机当前频道；
- `/clear` 不改变测向机当前频道；
- 非法坐标不会推进虚拟时间；
- 坐标每个分量的绝对值不能超过 $2\,000\,000$；
- 成功调用 `/enter` 后开始计时；
- 完成任务后应主动调用 `/exit`；
- 程序不能出现死循环；
- 需要同时考虑 $20$ 分钟程序时间和 $25$ 分钟测试窗口限制。

---

# 八、论文建议结构

1. 问题分析与基本假设；
2. 示向度误差带模型；
3. 问题1：定位区域及直径算法；
4. 问题2：第二检测点优化模型；
5. 问题3：全向干扰源搜索清除算法；
6. 问题4：定向干扰源鲁棒检测算法；
7. 移动、频道切换与清除时间模型；
8. 模拟器接口和程序流程；
9. 演练测试结果与参数调整；
10. 正式测试结果；
11. 敏感性分析、模型优点与不足。

## 九、核心建模思想总结

- 问题1解决“如何根据多个示向度确定目标区域”；
- 问题2解决“下一次应该在哪里检测以提高定位精度”；
- 问题3解决“在全向传播条件下如何高效搜索和清除”；
- 问题4解决“检测结果存在方向性和不确定性时，如何主动消除不确定性”。

在程序设计中，应优先保证清除干扰源的数量，再优化平均定位清除时间；在问题4中尤其不能把一次 `no_signal` 结果直接当成该频道不存在。
