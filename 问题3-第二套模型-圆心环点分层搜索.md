# 问题3第二套模型：圆心—环点分层搜索与逐源清除模型

## 1. 模型思路概述

问题3中所有干扰源均为全向干扰源。机器狗不预先知道干扰源的数量、频道和位置，只能通过检测逐步获取信息。

本模型采用与第一套“边搜索、边定位、边清除”不同的分层策略：

> **先整体扫描频道，建立候选频道清单；再逐个频道进行两点交会定位；最后按照定位精度和移动代价安排清除。**

整体流程为：

```text
圆心和环形检测点布置
        ↓
20个频道逐一扫描
        ↓
建立“可能存在干扰源”的频道清单
        ↓
对每个候选频道选择两个合适的检测点
        ↓
交会定位，得到目标估计位置
        ↓
按定位区域大小和距离排序
        ↓
巡游至估计位置附近执行清除
        ↓
清除失败则就地补测、修正位置后再次清除
```

本模型的核心是把“发现目标”和“处理目标”分开，使搜索阶段和清除阶段的任务更加清楚。

---

## 2. 基本符号

设目标区域为半径 $R=1800$ 米的圆域：

$$
\mathcal D=\{(x,y):x^2+y^2\leq R^2\}
$$

机器狗速度为 $v=5$ 米/秒，频道集合为：

$$
\mathcal C=\{1,2,\ldots,20\}
$$

每个频道最多对应一个干扰源。对频道 $c$，定义：

- $S_c$：该频道的候选检测点集合；
- $O_c$：该频道已获得的有效观测集合；
- $P_c$：该频道干扰源的定位区域；
- $\hat G_c$：该频道干扰源的位置估计；
- $d_c$：定位区域直径；
- $T_c$：清除频道 $c$ 所需的时间。

每条有效观测记录为：

$$
O=(x_s,y_s,\theta_s)
$$

其中 $(x_s,y_s)$ 是检测点坐标，$\theta_s$ 是测得的示向度。

---

## 3. 第一阶段：圆心和环形检测点扫描

### 3.1 检测点布置

首先在目标区域内设置一个圆心检测点和若干个环形检测点。

圆心检测点为：

$$
S_0=(0,0)
$$

环形检测点统一设置在半径 $r_s$ 的圆周上。若设置 3 个环形点，则取角度：

$$
\varphi_j=0^\circ,120^\circ,240^\circ
$$

对应检测点为：

$$
S_j=\left(r_s\cos\varphi_j,r_s\sin\varphi_j\right),
\quad j=1,2,3
$$

其中 $r_s$ 可以在演练测试中调节。建议初始取 $r_s=1200$ 米或 $1500$ 米，并比较两种半径下的发现率和总时间。

推荐优先使用 3 个环形点，因为三个点沿圆周均匀分布，能够从三个方向观察目标，比只使用两个环形点更不容易出现搜索盲区。

检测点布置示意：

```text
                         S2
                          ●
                         /
                        /
              S3 ●──── O ────● S1
```

其中 $O$ 为圆心。

### 3.2 逐点扫描 20 个频道

机器狗按照既定路线依次到达 $S_0,S_1,S_2,S_3$，在每个检测点依次检测频道 $1$ 至 $20$。

对每个检测请求，记录：

- 检测点坐标；
- 频道编号；
- `measure_result`；
- 若结果为 `direction`，记录 `svd_deg`；
- 当前虚拟时间；
- 当前机器狗位置。

检测结果的处理规则如下：

| 检测结果 | 处理方式 |
|---|---|
| `direction` | 将该频道加入候选频道清单，并保存位置与示向度 |
| `near` | 说明目标距离不超过 5 米，立即执行 `/clear` |
| `no_signal` | 本次检测点未收到信号，暂不判定该频道不存在 |

需要强调：由于问题3中目标是全向干扰源，`no_signal` 主要表示距离超过有效接收半径，或者该频道没有未清除目标。但单次 `no_signal` 仍不能作为绝对不存在的证明，因此候选频道清单应根据多个检测点综合确定。

### 3.3 候选频道清单

定义候选频道集合：

$$
\mathcal C^+
=\{c\in\mathcal C:\text{频道 }c\text{ 至少在一个检测点返回 direction 或 near}\}
$$

对每个频道记录其观测次数：

$$
N_c=|O_c|
$$

按照观测次数将频道分为：

- $N_c=0$：暂未发现；
- $N_c=1$：已发现，但暂时不能稳定定位；
- $N_c\geq2$：可以进行交会定位；
- `near`：已接近目标，可以直接尝试清除。

第一阶段的输出不是所有干扰源的精确位置，而是一个候选频道清单：

$$
\mathcal L=\{(c,O_c):c\in\mathcal C^+\}
$$

---

## 4. 第二阶段：逐频道两点交会定位

### 4.1 两点选择原则

对每一个候选频道 $c$，从已有检测点中选择两个观测点 $S_a,S_b$ 进行定位。

选择时同时考虑三个因素：

1. 两点都对频道 $c$ 检测到 `direction`；
2. 两个观测方向的交会角尽量接近 $90^\circ$；
3. 两点之间的移动距离不能过大。

设两个检测点处的示向度分别为 $\theta_a$ 和 $\theta_b$，目标的估计交会角为 $\alpha_{ab}$。定义两点质量评价函数：

$$
Q_{ab}
=
\lambda_1\sin\alpha_{ab}
-
\lambda_2\frac{\|S_a-S_b\|}{R}
$$

其中 $\lambda_1,\lambda_2$ 为权重。优先选择 $Q_{ab}$ 较大的点对。

简化时，可以采用以下规则：

- 优先选择交会角在 $60^\circ$ 至 $120^\circ$ 之间的点对；
- 若有多个点对满足条件，选择距离最近的点对；
- 若只有一个有效观测，则按照问题2的策略设计第二检测点。

### 4.2 只有一个观测时的第二检测点

若频道 $c$ 只有一个有效观测：

$$
O_c=\{(S_1,\theta_1)\}
$$

则根据问题2选择第二检测点 $S_2$：

- 不沿原示向度方向继续直线前进；
- 尽量从侧方观察目标；
- 使第二次观测和第一次观测的交会角接近 $90^\circ$；
- 优先选择移动距离较短且仍可能收到信号的位置。

可以采用如下构造：

1. 沿第一次示向度方向前进距离 $d_1$；
2. 再沿垂直方向偏移距离 $d_2$；
3. 到达第二检测点后，继续检测频道 $c$。

例如：

$$
S_2
=S_1+d_1(\cos\theta_1,\sin\theta_1)
+d_2(-\sin\theta_1,\cos\theta_1)
$$

其中 $d_1,d_2$ 可通过演练测试调整。初始可取 $d_1=600$ 米、$d_2=300$ 米。

### 4.3 构造定位区域

对频道 $c$ 的每条观测，将示向度误差考虑为 $\pm1^\circ$，得到一个角度扇区。

设第 $i$ 个观测点为 $S_i=(x_i,y_i)$，示向度为 $\theta_i$，则目标必须位于：

$$
\theta_i-1^\circ
\leq
\arg(G-S_i)
\leq
\theta_i+1^\circ
$$

所有观测对应扇区的交集为该频道的定位区域：

$$
P_c=\bigcap_{i\in O_c}W_i
$$

将每个扇区转换为两个半平面后，使用半平面交算法求出 $P_c$。

### 4.4 位置估计和定位精度

取定位区域 $P_c$ 的质心作为干扰源位置估计：

$$
\hat G_c=(\hat x_c,\hat y_c)
$$

同时计算定位区域直径：

$$
D_c=\max_{P,Q\in P_c}\|P-Q\|
$$

$D_c$ 越小，说明定位越精确，清除成功率越高。

建议设置两个定位精度等级：

| 定位区域直径 | 处理方式 |
|---:|---|
| $D_c\leq40$ 米 | 直接前往估计位置尝试清除 |
| $D_c>40$ 米 | 增加一个侧向检测点，再重新交会定位 |

阈值 40 米是经验阈值，最终应根据演练数据调整。由于清除半径为 20 米，若定位区域直径超过 40 米，直接使用质心清除的成功率可能不足。

---

## 5. 第三阶段：按顺序巡游清除

### 5.1 清除目标排序

获得多个候选频道的位置估计后，不立即按照频道编号清除，而是建立清除任务集合：

$$
\mathcal T=\{(c,\hat G_c,D_c):c\in\mathcal C^+\}
$$

对每个目标设置优先级。优先级应同时考虑定位精度、移动距离和清除难度。

一种简单的综合代价为：

$$
J_c
=\mu_1D_c
+\mu_2\|X-\hat G_c\|
+\mu_3\,\text{retry}_c
$$

其中：

- $X$ 为机器狗当前位置；
- $D_c$ 为定位区域直径；
- $\|X-\hat G_c\|$ 为移动距离；
- $\text{retry}_c$ 为该频道已经尝试清除的次数。

实际执行时采用以下排序原则：

1. 定位区域较小的目标优先；
2. 定位区域相近时，距离机器狗较近的目标优先；
3. 清除失败次数较多的目标暂时降低优先级；
4. 已经确认成功的频道从任务集合中删除。

为了兼顾效率，可以使用：

$$
\text{priority}_c
=\frac{1}{D_c+\varepsilon}
\cdot\frac{1}{\|X-\hat G_c\|+\varepsilon}
$$

优先处理 `priority` 较大的目标。

### 5.2 巡游清除

机器狗从当前点出发，依次前往各频道的估计位置，并调用 `/clear`：

```text
当前位置 X
    ↓
前往定位区域最小或较小的目标
    ↓
调用 /clear(channel=c, position=估计位置)
    ↓
返回 success？
    ├─ 是：标记频道 c 已清除
    └─ 否：执行就地补测和位置修正
```

使用 `/clear` 直接移动到估计位置，可以将“移动”和“清除”合并为一个接口动作，不需要额外使用 `/measure` 进行纯移动。

### 5.3 清除成功判定

对于频道 $c$，若返回：

```text
clear_result = "success"
```

则将该频道标记为已清除：

$$
\text{status}_c=\text{cleared}
$$

若返回：

```text
clear_result = "no_target_in_range"
```

则说明当前位置距离目标超过 20 米，不能把该频道直接判定为不存在，应进入位置修正流程。

---

## 6. 清除落空后的就地精修

### 6.1 精修原因

清除落空通常说明：

- 交会定位误差仍然较大；
- 两个检测点交会角不理想；
- 定位区域质心不是最接近真实目标的位置；
- 示向度误差使估计位置偏离目标超过 20 米。

因此，清除失败后不重新开始全区域搜索，而是在当前位置附近进行局部修正。

### 6.2 局部补测策略

设当前清除位置为 $X$，可以在 $X$ 附近选择四个或八个方向进行补测：

$$
X_k=X+r(\cos\phi_k,\sin\phi_k)
$$

其中：

- $r$ 初始可取 30 至 50 米；
- $\phi_k$ 可取 $0^\circ,90^\circ,180^\circ,270^\circ$；
- 若仍未定位成功，再增加 $45^\circ$、$135^\circ$ 等方向。

补测时只检测当前失败频道 $c$，不再扫描全部 20 个频道，以节约时间。

如果补测得到 `direction`，将新观测加入 $O_c$：

$$
O_c\leftarrow O_c\cup\{(X_k,\theta_k)\}
$$

然后重新计算：

$$
P_c,\quad \hat G_c,\quad D_c
$$

最后再次调用 `/clear`。

### 6.3 精修终止条件

满足以下任意条件时停止精修：

1. `/clear` 返回 `success`；
2. 补测次数达到上限，例如 3 次；
3. 剩余时间不足以完成一次补测和清除；
4. 定位区域连续两次没有明显缩小。

达到补测上限仍未成功时，将该频道标记为“待后处理”，先清除其他已经高精度定位的目标，避免一个难目标占用全部时间。

---

## 7. 时间模型

### 7.1 单次检测时间

一次 `/measure` 的耗时为：

$$
T_{measure}
=
\frac{d}{5}+t_{switch}+5
$$

其中：

- $d$ 为机器狗从上一个合法动作位置到当前检测点的距离；
- $t_{switch}=1$ 秒，当检测频道与当前测向机频道不同时取 1，否则取 0；
- 检测动作固定耗时 5 秒。

### 7.2 单次清除时间

一次 `/clear` 的耗时为：

$$
T_{clear}
=
\frac{d}{5}+t_{laser}
$$

其中：

$$
 t_{laser}=\begin{cases}
 5\text{ 秒},&\text{清除成功};\\
 3\text{ 秒},&\text{未发现目标}.
 \end{cases}
$$

### 7.3 总时间

本模型总定位清除时间为：

$$
T_{total}
=T_{scan}+T_{locate}+T_{巡游}+T_{refine}+T_{clear}
$$

其中：

- $T_{scan}$：圆心和环形检测点扫描所有频道的时间；
- $T_{locate}$：为完成第二次观测和交会定位所消耗的时间；
- $T_{巡游}$：各目标之间的移动时间；
- $T_{refine}$：清除失败后的局部精修时间；
- $T_{clear}$：成功清除动作耗时。

程序必须同时关注现实时间和虚拟时间。`/enter` 返回的 `remaining_real_duration_s` 是本局实际可使用的现实时间，不能固定假设每局均为 1200 秒。

---

## 8. 算法流程

```text
算法：圆心—环点分层搜索与逐源清除

输入：目标区域半径 R，频道集合 C={1,...,20}
输出：已清除频道集合 Cleared

1. 设置圆心 S0 和 3 个环形检测点 S1,S2,S3
2. Cleared ← ∅，Candidate ← ∅
3. 对检测点 S0,S1,S2,S3 依次执行：
4.     对频道 c=1,...,20：
5.         调用 /measure(position=Sj, channel=c)
6.         若返回 direction：
7.             保存 (Sj, svd_deg) 到 O_c
8.             将 c 加入 Candidate
9.         若返回 near：
10.            立即调用 /clear(position=Sj, channel=c)
11.            若成功，将 c 加入 Cleared
12. 对 Candidate 中每个频道 c：
13.     若 O_c 只有一个观测：
14.         按问题2选择第二检测点并补测
15.     若 O_c 至少有两个观测：
16.         选择交会角较好的两个观测点
17.         计算定位区域 P_c、估计位置 G_hat_c 和直径 D_c
18. 将候选目标按照 D_c 和当前位置距离排序
19. 对排序后的每个频道 c：
20.     若 c 已清除：跳过
21.     前往 G_hat_c，并调用 /clear
22.     若返回 success：
23.         将 c 加入 Cleared
24.     否则：
25.         在当前位置附近只检测频道 c
26.         加入新观测并重新交会定位
27.         在最大精修次数内重复 /clear
28. 若仍存在未清除频道且时间允许：
29.     对未充分确认的频道进行补充巡游扫描
30. 任务结束前调用 /exit
31. 返回 Cleared
```

---

## 9. 参数建议与演练调整

以下参数不应一次固定死，应通过演练测试调整：

| 参数 | 初始建议 | 调整依据 |
|---|---:|---|
| 环形检测点数量 | 3 | 漏检率和扫描时间 |
| 环形半径 $r_s$ | 1200 或 1500 米 | 候选频道发现率 |
| 第二检测点前进距离 $d_1$ | 600 米 | 交会角和信号接收率 |
| 第二检测点侧向距离 $d_2$ | 300 米 | 定位区域直径 |
| 直接清除阈值 | $D_c\leq40$ 米 | 清除成功率 |
| 局部精修半径 | 30 至 50 米 | 清除落空后的成功率 |
| 最大精修次数 | 3 次 | 时间消耗和成功率 |
| 最后补充搜索预留时间 | 180 秒 | 演练中的漏检情况 |

建议每次演练记录：

- 干扰源总数；
- 候选频道数；
- 实际清除频道数；
- 初始扫描阶段发现的频道数；
- 清除成功率；
- 平均定位清除时间；
- 清除失败次数；
- 最终补充搜索发现的目标数。

---

## 10. 模型优点与不足

### 10.1 优点

1. **结构清晰**：先发现频道，再定位，再清除，便于实现和调试。
2. **降低重复扫描**：完成初始频道筛选后，后续主要检测候选频道，减少无效的 20 频道重复扫描。
3. **定位质量可量化**：使用定位区域直径 $D_c$ 衡量每个目标的不确定性。
4. **清除失败可恢复**：落空后只对当前频道局部补测，不必重新搜索整个区域。
5. **便于参数优化**：环形半径、补测距离和精修次数都可以通过演练数据调整。

### 10.2 不足及改进方向

1. **圆心加少量环点不能严格保证一次发现全部干扰源**：因此必须保留补充巡游扫描阶段。
2. **初始阶段全频道扫描可能耗时较多**：可以在后续检测点只扫描未确认频道和单次观测频道。
3. **仅用两点交会可能受误差影响**：若时间允许，应使用第三个观测点进行验证。
4. **清除任务排序是启发式的**：可以在后续版本中使用最近邻、2-opt 或带权路径规划进一步缩短巡游时间。
5. **候选频道的 `no_signal` 不能被立即判定为不存在**：应结合多个扫描点和最终清除结果判断。

---

## 11. 论文中的总结表述

本文针对问题3提出一种圆心—环点分层搜索模型。首先在目标区域中心和环形检测点对 20 个频道进行粗粒度扫描，利用 `direction` 和 `near` 结果建立候选频道集合；随后针对每个候选频道，依据问题2中的第二检测点选择策略补充观测，使两条示向度方向具有较好的交会角，并通过误差扇区求交获得干扰源定位区域。最后，根据定位区域直径和移动距离对候选目标排序，机器狗依次巡游至各目标估计位置执行清除。若清除返回 `no_target_in_range`，则在当前位置附近对该频道进行局部补测，更新定位区域后再次清除。该模型将全局搜索、精确定位和局部修正结合起来，在保证策略可解释性的同时兼顾了清除成功率和总耗时。
