# 问题3：全向干扰源自动搜索定位与清除策略

## 一、问题分析

目标区域为半径1800米的圆形区域，其中有10-16个全向干扰源，每个干扰源频道互不相同（1-20中的若干个），有效接收半径1000-1500米。机器狗从原点出发，初始频道为1，需要在确保所有干扰源被清除的前提下，使完成任务的时间最短。

**核心挑战**：
1. 干扰源数量和位置未知，需要搜索发现；
2. 单个检测点只能获得示向度（方向），无法确定距离，需要至少2个检测点交会定位；
3. <span style="color:red">【修正】程序运行时间最长20分钟（1200秒）是现实时间限制，不是虚拟时间的预算：虚拟时间上限为100小时（360000秒），基本不构成约束。因此规划目标是使虚拟时间（平均定位清除时间）尽量短，而不是把虚拟耗时压在1200秒以内；现实时间只需保证请求总量不超限（每次请求毫秒级往返，绰绰有余）。</span>
4. 清除需要距离干扰源≤20米，定位误差可能导致清除失败，需要位置修正机制。

**全向干扰源的优势**：信号360°均匀辐射，只要在有效接收半径内就能检测到信号，无需考虑定向覆盖角度。

## 二、整体策略

采用**"搜索-定位-清除"三阶段动态迭代策略**，核心思想是：发现一个、定位一个、清除一个，在清除过程中从新位置继续搜索，最大化信息获取效率。

```
┌─────────────────────────────────────────────────┐
│              主循环（时间未耗尽）                  │
│  ┌───────────┐   ┌───────────┐   ┌──────────┐ │
│  │ 1.批量扫描 │→ │ 2.交会定位 │→ │ 3.选择目标 │ │
│  │  (当前位置) │   │ (更新估计) │   │ (最近优先) │ │
│  └───────────┘   └───────────┘   └────┬─────┘ │
│         ↑                                │       │
│         │                          ┌─────▼─────┐ │
│         │                          │ 4.逼近清除 │ │
│         │                          │ (移动+清除) │ │
│         │                          └─────┬─────┘ │
│         │                                │       │
│         │  无已知目标时                   │       │
│         └──────── 移动到下一个搜索点 ◄────┘       │
└─────────────────────────────────────────────────┘
```

### 阶段1：批量扫描（发现频道）

在当前位置对所有未清除频道依次检测。每次检测消耗5秒，频道切换消耗1秒。

- **初始位置（原点）**：扫描全部20个频道，发现有效接收半径内的干扰源；
- **后续位置**：优先扫描尚未发现的频道和只有单次观测的频道<span style="color:red">【补充】实现上：仅在原点与搜索航点做全量扫描（全部未清除频道），其余位置只扫描观测次数不足2次的频道，避免重复检测已定位频道</span>；
- 检测结果分为三种：`direction`（有信号，记录示向度）、`near`（距离≤5米，直接清除）、`no_signal`（无信号）。

### 阶段2：交会定位（确定位置）

当某个频道在2个或以上检测点获得示向度后，使用问题1的半平面交算法进行交会定位：

1. 将每个观测（位置+示向度）转化为2个半平面（示向度±1°扇区）；
2. 所有半平面取交集，得到凸多边形定位区域<span style="color:red">【修正】初始裁剪区域取1800米圆域的内接正96边形（与问题1修正后的方案一致，不用矩形包围盒，避免区域被截断或假有界）</span>；
3. 取定位区域的质心作为干扰源位置的估计值；
4. 记录定位区域直径，作为定位不确定性的度量。

### 阶段3：目标选择（贪心策略）

按以下优先级选择下一个要处理的目标频道：

1. **已有定位估计且距离当前位置最近**的频道——直接移动清除；
2. **只有单次观测**的频道——移动到第二个检测点进行二次定位；
3. 没有已知目标时——移动到下一个搜索位置继续扫描。

距离计算使用当前位置到估计位置的直线距离。

### 阶段4：逼近与清除

**已有定位估计时**：
- 直接使用 `/clear` 指令移动到估计位置并尝试清除（移动+清除一次指令完成，节省5秒检测时间）；
- <span style="color:red">【补充】若定位区域直径超过40米（CLEAR_DIAM_GATE），在最近观测站的示向度方向横向取点补测（沿示向度方向取点会与既有观测共线、交角≈0°无信息量；横向取点与二次检测点同设计，保证交角大且必能收到信号），再尝试清除；</span>
- 清除半径20米，若估计位置误差≤20米则清除成功；
- 若清除失败（`no_target_in_range`），触发位置修正机制。

**只有单次观测时**：
- 沿示向度方向前进约600米，再垂直偏移约300米，选择第二个检测点；
- 使用 `/measure` 指令移动到第二个检测点并检测目标频道（移动+检测一次完成）；
- 获得第二次示向度后进行交会定位，然后移动清除。

### 位置修正机制

清除失败说明估计位置偏差>20米。修正方法：
1. 在当前位置重新检测该频道，获得新的示向度；
2. 将新观测加入观测集合，重新进行交会定位；
3. 移动到新的估计位置<span style="color:red">【修正】并立即再次尝试清除（原实现只移动到新估计不尝试，会多花一轮全量扫描后才清除）</span>；
4. 最多重试3次，超过后暂时放弃该频道，处理其他目标<span style="color:red">【补充】放弃后若在搜索航点等位置获得新观测，重新给予清除机会（避免信息已充分却永久放弃）</span>。

### 搜索补漏策略

当所有已知频道都处理完后，需要搜索可能遗漏的干扰源：

1. **搜索航点**：预定义13个搜索位置——原点 + 6个方向（间隔60°）×2个距离（900米、1500米）；
2. 选择距离当前位置最近且未访问过的航点；
3. 移动到该航点后进行批量扫描；
4. 搜索航点的设计确保覆盖整个1800米区域（有效接收半径≥1000米，正六边形网格覆盖）。

### 时间管理

<span style="color:red">【修正】时间管理采用"现实/虚拟"双时钟模型（原稿把两者混为一谈）：</span>

- <span style="color:red">【修正】现实时钟：`/enter` 返回的 `remaining_real_duration_s`（最多1200秒）是现实时间，程序内用 `time.monotonic()` 追踪；现实剩余不足30秒（REAL_EXIT_RESERVE）时停止新操作并调用 `/exit` 收尾，防止被模拟器超时掐断；</span>
- <span style="color:red">【修正】虚拟时钟：`virtual_time_s` 由模拟器返回，上限360000秒（100小时），基本不构成约束；它是"平均定位清除时间"评价指标的来源，是优化目标而不是紧约束；</span>
- **预留时间**：<span style="color:red">【修正】现实剩余不足60秒（TIME_RESERVE）不再开启新的二次定位目标，只处理已定位的目标</span>；
- 每次操作前检查现实剩余时间，不足时停止当前操作；
- 清除成功的频道不再检测，节省时间。

## 三、算法设计

### 3.1 主算法伪代码

```
算法：自动搜索定位清除
输入：模拟器接口（/enter, /measure, /clear, /exit）
输出：清除的干扰源列表、总时间

1. 调用 /enter 进入目标区域，获取剩余时间 T_max
2. 初始化：当前位置=(0,0)，当前频道=1，已清除集合=∅，观测字典=∅
3. 循环（<span style="color:red">【修正】现实剩余时间 > 30秒，且虚拟时间 < 360000秒</span>）：
   a. 在当前位置批量扫描<span style="color:red">【修正】原点与搜索航点全量扫描；其余位置只扫描观测不足2次的频道</span>，更新观测字典
   b. 对有≥2次观测的频道执行交会定位，更新估计位置
   c. 选择目标频道（已定位最近优先，其次单次观测）
   d. 若有目标：
      - 已定位：直接 /clear 移动到估计位置并清除
      - 单次观测：/measure 移动到第二个检测点并检测，然后定位清除
      - 清除失败时执行位置修正（最多3次）
   e. 若无目标且<span style="color:red">【修正】现实剩余时间 > 60秒</span>：
      - 移动到最近的未访问搜索航点
   f. 若已清除≥16个，提前结束
4. 调用 /exit 退出
5. 输出统计结果
```

### 3.2 交会定位算法（复用问题1）

```
算法：locate_from_observations(observations)
输入：观测列表 [(x1,y1,θ1), (x2,y2,θ2), ...]
输出：估计位置 (x,y)、定位区域直径、顶点列表

1. 对每个观测，生成2个半平面（示向度±1°扇区的左右边界）
2. 半平面交（增量切割法<span style="color:red">【修正】初始区域为1800米圆域内接正96边形，与问题1修正后的方案一致</span>）→ 凸多边形顶点
3. 计算多边形质心 → 估计位置
4. 暴力枚举顶点对 → 定位区域直径
5. 返回（估计位置，直径，顶点）
```

### 3.3 第二个检测点选择算法（复用问题2结论）

```
算法：select_second_station(pos1, theta1)
输入：第一个检测点位置 pos1，示向度 theta1
输出：第二个检测点位置 pos2

1. 沿示向度方向前进 600 米 → 中点 mid
2. 垂直偏移 300 米（示向度方向左侧）→ pos2
3. 返回 pos2
```

设计依据（问题2结论）：
- 交角α≈90°时定位精度最高；
- 沿示向度方向前进600米后垂直偏移300米，对于距离800-1200米的干扰源，交角约为 atan(300/200)≈56° 到 atan(300/600)≈27°，保证有效交会；
- 偏移量可根据实际情况调整。

## 四、时间分析

### 4.1 单次操作耗时

| 操作 | 耗时组成 | 典型耗时 |
|------|----------|----------|
| 检测（不移动） | 5秒检测 + 频道切换 | 5-6秒 |
| 检测（移动距离d） | d/5秒移动 + 5秒检测 + 切换 | d/5 + 6秒 |
| 清除（移动距离d，成功） | d/5秒移动 + 5秒清除 | d/5 + 5秒 |
| 清除（移动距离d，失败） | d/5秒移动 + 3秒清除 | d/5 + 3秒 |

### 4.2 典型场景时间估算

<span style="color:red">【修正】原稿把虚拟耗时与1200秒现实限额直接比较（"1580秒→超出1200秒"），概念错误：1200秒是现实时间的上限（约束的是现实内的请求总量），虚拟时间上限为360000秒。虚拟时间的大头是移动（100米=20秒），扫描很便宜（检测5秒+切换1秒）。按虚拟时间重新估算（假设12个干扰源）：</span>

1. **初始扫描**：原点检测20个频道 ≈ 5 + 19×6 = 119秒
2. **每个干扰源的定位清除**：
   - 若初始扫描已发现（2次观测来自初始扫描+移动中检测）：
     - 移动到估计位置（平均500米）+ 清除 ≈ 100 + 5 = 105秒
   - 若需要二次定位：
     - 移动到第二个检测点（平均400米）+ 检测 ≈ 80 + 6 = 86秒
     - 移动到估计位置（平均300米）+ 清除 ≈ 60 + 5 = 65秒
     - 合计 ≈ 151秒
3. **补搜**：访问2-3个搜索航点<span style="color:red">【修正】原稿此处文字写"480秒"而汇总式写"200"，数字矛盾且偏大：航点就近访问，平均移动约400米/次</span>：
   - 移动 2-3 × 400米 ≈ 240秒
   - 航点全量扫描 2-3 × 120 ≈ 300秒

**估算**（大部分干扰源在初始扫描中发现，清除顺利）：
119 + 12×105 + 240 + 300 ≈ 1900秒虚拟时间<span style="color:red">【修正】远小于360000秒上限，不构成约束；相应请求约200-300次，每次毫秒级往返，现实耗时远小于1200秒</span>。

<span style="color:red">【修正】删去"减少初始扫描的频道数（先扫10个）"的错误建议——扫描便宜且是发现干扰源的唯一手段，减少扫描只会漏源。真正的优化方向：</span>
- 移动与检测/清除合并（/measure、/clear 一次指令完成移动+操作）；
- 非航点位置只扫描观测不足2次的频道（避免重扫已定位频道）；
- 定位直径超过40米时先到估计位置补测再清除（避免大概率失败的清除尝试）；
- 航点访问顺序按最近优先，减少无效移动；
- 优先处理距离近的干扰源。

当前程序已实现上述优化，实际效率需通过模拟器测试验证。

## 五、关键优化点

1. **移动+操作合并**：使用 `/measure` 移动时同时完成检测，使用 `/clear` 移动时同时完成清除，避免单独的移动指令浪费时间。

2. **动态频道扫描**：已清除的频道不再检测，减少无效检测次数。

3. **贪心目标选择**：始终选择距离当前位置最近的已定位目标，减少移动距离。

4. **位置修正**：清除失败后立即在原地检测修正，避免盲目移动。

5. **搜索航点预定义**：6方向×2距离的航点设计，确保覆盖整个区域，避免重复访问。

6. **时间预留**：<span style="color:red">【修正】现实剩余60秒不再开新目标、剩余30秒收尾退出（代码 TIME_RESERVE / REAL_EXIT_RESERVE）</span>，避免因现实超时被模拟器掐断。

## 六、测试结果

（待模拟器演练测试和正式测试后补充）

### 6.1 演练测试统计

<span style="color:red">【补充】2026-09-11、09-12 连接真实模拟器演练7局（队号202610061109），全部清除、无漏源：</span>

| 局次 | 清除 | 虚拟时间 | 现实耗时 | 平均定位清除时间 |
|------|------|----------|----------|------------------|
| <span style="color:red">1</span> | <span style="color:red">12/12</span> | <span style="color:red">6929.5 s</span> | <span style="color:red">2.1 s</span> | <span style="color:red">577.5 s</span> |
| <span style="color:red">2</span> | <span style="color:red">13/13</span> | <span style="color:red">7445.5 s</span> | <span style="color:red">1.6 s</span> | <span style="color:red">572.7 s</span> |
| <span style="color:red">3</span> | <span style="color:red">11/11</span> | <span style="color:red">6301.1 s</span> | <span style="color:red">1.9 s</span> | <span style="color:red">572.8 s</span> |
| <span style="color:red">4</span> | <span style="color:red">13/13</span> | <span style="color:red">7174.5 s</span> | <span style="color:red">1.8 s</span> | <span style="color:red">551.9 s</span> |
| <span style="color:red">5</span> | <span style="color:red">12/12</span> | <span style="color:red">6064.8 s</span> | <span style="color:red">2.0 s</span> | <span style="color:red">505.4 s</span> |
| <span style="color:red">6</span> | <span style="color:red">14/14</span> | <span style="color:red">6568.5 s</span> | <span style="color:red">1.8 s</span> | <span style="color:red">469.2 s</span> |
| <span style="color:red">7</span> | <span style="color:red">11/11</span> | <span style="color:red">6675.9 s</span> | <span style="color:red">2.0 s</span> | <span style="color:red">606.9 s</span> |

<span style="color:red">要点：</span>

- <span style="color:red">7 局共清除 86 个干扰源（11~14 档源数），每局扫完全部 13 个搜索航点后均无新发现，确认无漏源；</span>
- <span style="color:red">7 局共 10 次清除失败（每局 0~2 次），全部经位置修正后在 3 次上限内成功；第2局 10 次横向补测全部一次清除成功；</span>
- <span style="color:red">开场 /enter 连接被模拟器重置（倒计时未结束，最多约 30 次），轮询重试机制每局均正常渡过；</span>
- <span style="color:red">虚拟时间 6065~7446 秒，均远低于 100 小时上限。</span>

| 指标 | 数值 |
|------|------|
| 平均清除率 | <span style="color:red">【补充】100%（7局 86/86）</span> |
| 平均定位清除时间 | <span style="color:red">【补充】550.9 秒（每局 469.2 ~ 606.9 秒）</span> |
| 平均程序运行时间 | <span style="color:red">【补充】1.9 秒（1.6 ~ 2.1 秒）</span> |

### 6.2 正式测试结果（表1格式）

| 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---|---|
| 测试1 | 待测试 | 待测试 | 待测试 |
| 测试2 | 待测试 | 待测试 | 待测试 |
| 测试3 | 待测试 | 待测试 | 待测试 |

## 七、程序使用说明

### 7.1 文件结构

- `robot.py`<span style="color:red">【修正】原文件名为 robot(1).py，已重命名为 robot.py</span>：主程序，包含HTTP客户端与策略逻辑
- `geometry.py`<span style="color:red">【补充】本仓库内新建，接口与 robot.py 配套；内部复用 problem1_algorithm.py 的已验证实现</span>：问题1的半平面交和直径计算算法（复用）
- `problem1_algorithm.py`<span style="color:red">【补充】</span>：问题1算法实现（geometry.py 依赖，需与 robot.py 同目录）

### 7.2 运行步骤

1. 启动模拟器，用参赛队号登录；
2. 选择"问题3演练测试"或"问题3正式测试"，点击开始；
3. 等待5秒倒计时结束，界面显示"接口已就绪"；
4. 运行程序<span style="color:red">【修正】本机命令行为大写 Python</span>：`Python robot.py <参赛队号>`<span style="color:red">【补充】程序会自动轮询 /enter 接口，倒计时结束、接口就绪后自动开始，无需手工按键</span>；
5. 程序自动执行搜索、定位、清除，完成后自动退出；
6. 查看控制台输出和日志文件 <span style="color:red">【修正】`robot_logs/` 文件夹内编号日志 `robot_logN.txt`（每次运行自动新建编号日志，不覆盖历史日志；离线测试不写日志）</span>。

### 7.3 参数调优

程序中可调整的关键参数：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `TIME_RESERVE` | <span style="color:red">【修正】60秒（现实时间）</span> | <span style="color:red">现实剩余不足60秒时不再开启新的二次定位目标</span> |
| `REAL_EXIT_RESERVE` | <span style="color:red">【补充】30秒（现实时间）</span> | <span style="color:red">现实剩余不足30秒时停止操作并 /exit 收尾</span> |
| `MAX_CLEAR_RETRY` | 3次 | 单频道最大清除重试次数 |
| `MIN_OBSERVATIONS` | 2次 | 定位所需最少观测数 |
| `CLEAR_DIAM_GATE` | <span style="color:red">【补充】40米</span> | <span style="color:red">定位直径超过该值时先到估计位置补测再清除</span> |
| 二次定位移动距离 | 600米 | 沿示向度方向前进距离 |
| 二次定位垂直偏移 | 300米 | 垂直偏移距离 |
