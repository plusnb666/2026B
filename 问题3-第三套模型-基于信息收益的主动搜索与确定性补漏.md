# 问题3建模方案（第三套）：基于信息收益的主动搜索与确定性补漏

## 一、问题分析

### 1.1 任务与目标

目标区域为半径 1800 米的圆形区域，内部有 10-16 个全向干扰源，位置未知，频道互不相同（取自 1-20 频道）。机器狗从原点出发，初始频道为 1。一次检测（`/measure`）在干扰源有效接收半径（1000-1500 米）内可获得示向度（误差 ±1°），距离不超过 5 米时返回 `near`；清除（`/clear`）要求距干扰源不超过 20 米。

目标：**在保证全部干扰源被清除的前提下，最小化总定位清除时间（虚拟时间）**。题目要求记录三项指标：清除干扰源个数、平均定位清除时间、程序运行时间。

### 1.2 问题本质

第三问不是已知目标的路径规划问题，而是**未知多目标的在线主动搜索、定位与清除问题**：

- 未知：干扰源总数 $N\in[10,16]$、各源位置、哪些频道被占用；
- 在线：每次检测都改变机器狗对环境的认识，不能预先固定一条完整路线，必须根据当前检测结果动态决定下一步行动；
- 全向源特性：信号 360° 均匀辐射，接收半径不小于 1000 米，单点检测覆盖面积大，且 `no_signal` 结果具有强排除能力。

### 1.3 时间模型：现实/虚拟双时钟

<span style="color:red">【补充】原稿未区分两种时间：</span>

- **现实时间**：`/enter` 返回的 `remaining_real_duration_s`（0-1200 秒）是程序可运行的真实时长。它只约束"现实时间内能发出的请求总量"——每次请求毫秒级往返，整套流程约 200-300 次请求，实际占用仅数秒，基本不构成约束；但程序必须预留 30 秒调用 `/exit` 收尾，防止被模拟器超时掐断。
- **虚拟时间**：`virtual_time_s` 由模拟器推进（移动 d/5 秒、检测 5 秒、切换 1 秒、清除成功 5 秒/失败 3 秒），上限 360000 秒（100 小时）。它是"平均定位清除时间"这一评价指标的来源，是优化目标而不是紧约束。

## 二、总体策略框架

采用**"主动搜索 → 交会定位 → 精确清除 → 状态更新 → 继续搜索"的闭环策略，外加后期确定性补漏机制**，完整结构为：

$$\boxed{\text{概率驱动提高效率}\;+\;\text{确定性覆盖保证完整}}$$

- **搜索阶段**：用"单位时间信息收益"价值函数主动前往高价值检测点，尽快发现尽可能多的目标；
- **定位阶段**：发现信号后立即转入交会定位，复用问题1/问题2结论快速收缩候选区域；
- **清除阶段**：达到闸门即尝试清除，失败时利用失败信息修正；
- **补漏阶段**：主动搜索收益耗尽后，按确定性覆盖航点全频扫描，确保不漏任何干扰源。

## 三、频道状态建模

### 3.1 候选区域

对每个频道 $c=1,2,\ldots,20$ 维护候选区域 $\Omega_c$：根据当前全部检测结果，频道 $c$ 的干扰源**可能出现的位置集合**。初始时：

$$\Omega_c=B(0,1800),\qquad status_c=unknown$$

因为初始无法判断哪些频道对应干扰源。

### 3.2 状态机

$$status_c\in\{unknown,\ searching,\ localized,\ cleared,\ empty\}$$

- `unknown`：尚未进行过有效检测；
- `searching`：参与主动搜索评分；
- `localized`：已获得至少 1 次示向度，进入定位流程；
- `cleared`：已清除，$\Omega_c$ 从系统中删除；
- `empty`：$\Omega_c=\varnothing$，证明该频道在整个目标区域内不存在未清除的干扰源。

### 3.3 候选区域的离散化表示

<span style="color:red">【补充】原稿未说明 $\Omega_c$ 的计算机表示：</span>`no_signal` 的排除操作使 $\Omega_c$ 变为非凸区域，面积无法解析计算。实现时用边长 $\delta=10$ 米的正方形网格离散化 1800 米圆域（每频道约 $1.0\times10^5$ 个格点），$\Omega_c$ 用布尔矩阵表示，面积 $A(\cdot)$ 以格点数计；圆盘内格点计数用预计算的偏移表实现，单次更新复杂度为 $O(|\Omega_c|)$。

## 四、观测模型与候选区域更新

设机器狗在 $S=(x_s,y_s)$ 检测频道 $c$，按返回结果分四种情形更新：

### 4.1 `direction`：收缩候选区域

获得示向度 $\alpha_c$，测向误差 $\varepsilon_c\in[-1°,1°]$，故 $G_c\in W(S,\alpha_c)$，其中 $W$ 是问题1建立的角度误差区域。更新：

$$\boxed{\Omega_c\leftarrow\Omega_c\cap W(S,\alpha_c)}$$

### 4.2 `no_signal`：排除候选区域

干扰源均为全向源，若检测点与源的距离超过有效接收半径则收不到信号；又接收半径 $R_c\ge1000$ 米，故无信号给出保守排除约束：

$$no\_signal\;\Rightarrow\; d(S,G_c)>R_c\ge1000\;\Rightarrow\;\boxed{\Omega_c\leftarrow\Omega_c\setminus B(S,1000)}$$

即本次检测至少排除检测点周围半径 1000 米的圆盘。

### 4.3 `near`：直接清除

<span style="color:red">【补充】原稿完全遗漏了 `near` 结果：</span>附件规定距离不超过 5 米时返回 `near`，机器狗可立即清除该干扰源。此时在当前位置直接 `/clear`（5 米 < 20 米必成功），零移动成本：

<span style="color:red">$$near\;\Rightarrow\;clear(S,c)\;\Rightarrow\;status_c=cleared$$</span>

### 4.4 清除失败：负信息排除

<span style="color:red">【补充】原稿未利用清除失败带来的信息：</span>`/clear` 返回 `no_target_in_range` 说明 $d(p_{clear},G_c)>20$ 米，同样可排除一个小圆盘：

<span style="color:red">$$\boxed{\Omega_c\leftarrow\Omega_c\setminus B(p_{clear},20)}$$</span>

清除失败并非纯损失——它免费获得了一次强排除信息，配合原地补测（无移动）形成修正闭环（见 §6.4）。

## 五、搜索决策：检测点价值函数

### 5.1 空间覆盖价值

对候选检测点 $S$ 与待检频道集合 $C_S$，定义频道 $c$ 的潜在发现覆盖比例：

$$P_c(S)=\frac{A(\Omega_c\cap B(S,1000))}{A(\Omega_c)}$$

含义：假设源在 $\Omega_c$ 内均匀分布（建模假设，模拟器不提供分布信息），$P_c$ 是一次检测覆盖候选区域的比例。$\Omega_c=\varnothing$ 时定义 $P_c=0$。

### 5.2 时间代价

- 移动：机器狗速度 $v=5$ 米/秒，$T_{move}=\|S-X\|/5$；
- 检测：$T_{measure}=5\,|C_S|$；
- 切换：$T_{switch}=\sum_{j=1}^{m}I(c_j\ne c_{j-1})$，每次 1 秒。由于 $C_S$ 内频道互不相同，按"当前频道优先"排序后 $T_{switch}=|C_S|-I(c_0\in C_S)$。

### 5.3 综合价值函数

$$Score(S,C_S)=\frac{\sum_{c\in C_S}P_c(S)}{\dfrac{\|S-X\|}{5}+5|C_S|+T_{switch}}$$

分子是本次访问对多个频道产生的综合发现价值（移动成本只需付一次，检测多个频道摊薄成本），分母是总耗时；整体即**单位时间的预期空间覆盖收益**。

### 5.4 候选点生成

在 1800 米圆域内极坐标采样：半径 $\{400,800,1200,1600\}$ 米 × 12 方向（间隔 30°）再加原点，共 49 个候选点。对每个候选点，待检频道集合取 $\Omega_c\neq\varnothing$ 且 $\Omega_c\cap B(S,1000)\neq\varnothing$ 的未结论频道。

### 5.5 滚动决策

不预先规划完整路线，每次检测后根据最新状态重新评分：

$$\boxed{S_{k+1}=\arg\max_S\;Score(S,C_S\mid \text{当前信息})}$$

属典型的滚动决策（Receding Horizon）思想。

## 六、定位与清除

### 6.1 交会定位（衔接问题1）

某频道获得首次 `direction` 后立即进入 `localized` 状态，不再参与搜索评分，其下一步行动由本节决定。设已获得观测 $(S_1,\alpha_1),\ldots,(S_k,\alpha_k)$，则

$$\Omega_c^{(k)}=\bigcap_{j=1}^{k}W(S_j,\alpha_j)$$

用问题1的半平面交算法（1800 米圆域内接正 96 边形初始化）求凸多边形定位区域；取质心为估计位置，记区域直径为 $D$ 作为定位不确定度。

### 6.2 下一检测点选择（衔接问题2）

<span style="color:red">【修正】原稿只写 $\arg\max Q$，未给出具体构造。复用问题2已验证结论：</span>从最近观测站沿示向度方向前进 600 米，再垂直偏移 300 米，作为下一检测点。数值验证：

- 该点到干扰源的距离 ≤ 957.1 米（含 ±1° 误差）< 1000 米最小接收半径 ⇒ **二次检测必能收到信号**；
- 与首次观测的交角为 26.6°-56.3°（源距 1200 米/800 米时）⇒ 交会定位有效。

注意不能沿示向度方向取点——那样与既有观测共线、交角≈0°，新观测无信息量；横向偏移正是为获得大交角。

### 6.3 清除决策

清除的最终目的不是无限精确的坐标，而是找到一个保证清除成功的位置（≤20 米）。对 $\Omega_c$ 求最小包围圆 $(C_c^*,R_c^*)$，由 Jung 定理，平面凸区域直径 $D$ 满足 $R_c^*\le D/\sqrt3$，故 $D\le20\sqrt3\approx34.6$ 米时，圆心 $C_c^*$ 是理论保证的清除点。

<span style="color:red">【修正】原稿要求 $R_c^*\le20$ 米才清除——理论正确但代价过高：±1° 示向误差下把区域压到该尺度需要多次近距观测。工程上采用"闸门 + 修正"机制：</span>

- **闸门**：定位直径 $D\le40$ 米即用 `/clear` 前往质心估计位置尝试清除（移动+清除一次指令完成）；
- **补测**：若 $D>40$ 米，先到最近观测站示向度的横向补测点（同 600/300 设计，保证大交角且必能收到信号）补测一次再清除；
- **失败修正**（§6.4）：清除失败不是放弃，而是修正定位。

### 6.4 清除失败修正

```
失败 → Ω_c 排除 B(p_clear,20) → 原地检测该频道（无移动，5秒）
     → 新观测加入 → 重新交会定位 → 立即再次 /clear
```

最多重试 3 次，超过后暂时放弃该频道、处理其他目标；若后续在搜索航点等位置获得新观测（观测数增加），重新给予清除资格，避免信息已充分却永久放弃。

<span style="color:red">【补充】上述"闸门 + 横向补测 + 失败修正"机制已在真实模拟器演练 2 局验证：25/25 全部清除（虚拟时间 6929.5 / 7445.5 秒），其中失败修正触发 2 次均一次修正成功。</span>

## 七、完整性保障：确定性补漏

### 7.1 覆盖航点设计

<span style="color:red">【补充】原稿只说"继续寻找检测点使 $\Omega_c$ 被覆盖排除"，没有给出具体覆盖点集。采用 13 航点设计：</span>

$$\text{原点}\;+\;6\ \text{个方向（间隔}\ 60°\text{）}\times\{900,1500\}\ \text{米}$$

数值验证：1800 米圆域内任一点到最近航点的距离 ≤ 901.9 米 < 1000 米，故 13 个 $B(\text{航点},1000)$ 圆盘的并集覆盖整个目标区域。

### 7.2 补漏流程与终止条件

主动搜索的收益耗尽后（无未结论频道，或最高 Score 低于阈值），进入确定性覆盖阶段：按最近优先顺序访问未访问航点，在每个航点**全频扫描所有未结论频道**。全部航点访问完毕后：

- 某频道在所有航点均 `no_signal` ⇒ $\Omega_c=\varnothing$（被 13 个 1000 米圆盘完全覆盖排除）⇒ 该频道不存在未清除干扰源，标记 `empty`；
- 航点扫描新发现的 `direction` ⇒ 重新进入 LOCALIZE 流程。

**终止条件**：$\forall c,\ status_c\in\{cleared,\ empty\}$（或已清除 16 个，达到总数上限）。由此，价值函数只负责"快"，确定性覆盖负责"全"，正确性不依赖启发式。

## 八、算法流程

### 8.1 状态机

```
                    ┌──────────┐   首次 direction    ┌──────────────┐
      初始化 ─────→ │  SEARCH  │ ─────────────────→ │   LOCALIZE   │
                    └──────────┘                    └──────┬───────┘
                      ↑    │                               │ D≤40m
                      │    │ 收益耗尽/无未结论频道            ↓
                      │    ↓                          ┌──────────────┐
                    ┌──────────┐   全部频道结论明确    │    CLEAR     │
                    │ COVERAGE │ ←───────────────────│ (失败→修正)   │
                    └──────────┘                     └──────┬───────┘
                      │  航点全频扫描                          │ 成功
                      │  (Ω_c=∅ → empty)                     ↓
                      ↓                                status=cleared
                     结束                                   → SEARCH
```

### 8.2 主算法伪代码

```
算法：基于信息收益的主动搜索与确定性补漏
输入：模拟器接口 /enter /measure /clear /exit
1.  轮询 /enter 直至 accepted（倒计时期间连接可能被重置，需静默重试）
2.  初始化：位置 X=(0,0)，当前频道 c0=1；Ω_c=B(0,1800) 网格化；
    status_c=unknown；未访问航点集合；现实截止时刻
3.  while 现实剩余 > 30秒 且 虚拟时间 < 360000秒 且 未全部结论:
4.      if 存在 localized 频道:
5.          单次观测 → 二次检测点(600/300) /measure → 定位
6.          D > 40米 → 横向补测点 /measure 后重新判断
7.          D ≤ 40米 → /clear → 失败则修正重试(≤3次，新观测重新资格)
8.          continue
9.      for 每个候选点 S_i（49 点极坐标采样）: 计算 Score(S_i, C_i)
10.     if 无未结论频道 或 max Score 低于阈值 ε:
11.         if 航点已全部访问: 剩余频道标 empty；break
12.         按最近优先移动到下一航点，全频扫描（按 §4 更新 Ω 与状态）
13.     else:
14.         /measure 移动到 S*，检测 C*，按 §4 更新 Ω 与状态
15. end while
16. 现实剩余 < 60秒 不再开启新的二次定位；< 30秒 停止新操作
17. 调用 /exit；输出清除数、平均定位清除时间、程序运行时间
```

## 九、时间模型与评价指标

### 9.1 单次操作耗时

| 操作 | 耗时 |
|------|------|
| `/measure`（移动 d，频道相同） | d/5 + 5 秒 |
| `/measure`（移动 d，频道不同） | d/5 + 1 + 5 秒 |
| `/clear`（移动 d，成功） | d/5 + 5 秒 |
| `/clear`（移动 d，失败） | d/5 + 3 秒 |

### 9.2 总时间构成

<span style="color:red">【修正】原稿 §26.2 把含移动的 $T_{measure}$ 再与 $T_{move}$ 相加，重复计算。正确形式为按动作累加：</span>

<span style="color:red">$$\color{red}{T_{total}=\sum_{\text{所有动作}}\Big(T_{move}+T_{op}+T_{switch}\Big),\qquad T_{op}=\begin{cases}5,&measure\\5/3,&clear\ 成功/失败\end{cases}}$$</span>

### 9.3 评价指标

- 平均定位清除时间 = 虚拟总时间 ÷ 清除干扰源个数；
- 参考基线：第一套策略（固定航点贪心）演练 2 局平均 575.1 秒；本方案以价值函数替代固定航点访问顺序，预期进一步压缩移动耗时，实际效果待模拟器测试验证。<span style="color:red">【补充】2026-09-12 演练实测 484.8 秒，较基线提升约 15.7%，详见 §十三。</span>

## 十、接口约束与鲁棒性要求

<span style="color:red">【补充】原稿完全未涉及协议约束，实现时须满足：</span>

1. HTTP+JSON，地址 http://127.0.0.1:2026，端点 `/enter`、`/measure`、`/clear`、`/exit`；
2. 每个请求必带 `robot_id`（队号）、`arena_id="default"`、`request_id`；坐标绝对值 ≤ 2×10⁶，频道为 1-20 整数；
3. 每个新动作必须用新 `request_id`；仅网络重试可复用原请求与原 ID（幂等）；未知字段/结构错误返回 `accepted=false` 且不占用 ID；
4. 程序必须同时检查 HTTP 状态码与 `accepted` 字段；倒计时未结束或测试结束时连接可能被直接重置（演练实测约 30 次），`/enter` 应轮询直至成功；
5. 结束时必须调用 `/exit`。

## 十一、与问题1、问题2的衔接

- **问题1**：测向误差 ±1° → 角度误差区域 $W$ → 半平面交 → 定位区域与直径 $D$；
- **问题2**：已有候选区域 → 最优下一检测点（600 米前进 + 300 米垂直偏移）→ 区域快速收缩；
- **问题3**：未知 $N$ 与位置 → 维护 $\Omega_1,\ldots,\Omega_{20}$ → 以 $Score$ 主动搜索 → 发现目标后调用问题1+问题2 定位 → 闸门清除（失败修正）→ 确定性补漏 → 全部结论。

## 十二、模型特点与总结

本模型核心思想：**以信息收益/时间成本为导向主动搜索，以交会定位快速锁定目标，以确定性覆盖保证搜索完整性**。三个核心模块——主动搜索（快）、快速定位（准）、完整性保障（全）——组合出完整闭环。

相比固定航点贪心策略（第一套），本方案用价值函数替代固定访问顺序，理论上能优先覆盖"尚未排除的候选区域"，减少无效移动；相比遗传/蚁群等离线路径规划，本方案贴合题目的物理检测机制与在线决策特点，每个决策步只依赖当前信息，实现简单、鲁棒。

程序实现时可按七个模块组织：候选区域表示 → 检测点生成 → 价值函数计算 → 搜索状态机 → 问题1/问题2 定位 → 清除与修正 → 完整性检查。

## 参数表

| 参数 | 取值 | 说明 |
|------|------|------|
| 网格边长 δ | 10 米 | Ω_c 离散化 |
| 候选点采样 | 4 环 × 12 方向 + 原点 | 49 个候选检测点 |
| CLEAR_GATE | 40 米 | 定位直径闸门（理论保证阈值 34.6 米） |
| Score 阈值 ε | <span style="color:red">【修正】0.02（实现取值，可调）</span> | 低于该值进入补漏阶段 |
| MAX_RETRY | 3 次 | 清除失败修正次数上限 |
| 二次检测点 | 600 米前进 + 300 米垂直偏移 | 问题2结论 |
| 覆盖航点 | 13 个 | 最坏覆盖距离 901.9 米 |
| TIME_RESERVE | 60 秒（现实） | 不再开启新定位目标 |
| REAL_EXIT_RESERVE | 30 秒（现实） | 收尾调用 /exit |

## 十三、测试验证

<span style="color:red">【补充】2026-09-12 完成实现（robot_model3.py）与两级测试：</span>

### 13.1 离线端到端测试（假模拟器）

<span style="color:red">【补充】按附件2 规则在本地实现假模拟器（±1° 示向误差、虚拟时间推进、移动/检测/清除耗时同真实模拟器），10 个随机场景（10~16 个源，与第一套同种子便于对比）全部清除：</span>

| 场景 | 清除率 | 虚拟时间 |
|------|--------|----------|
| <span style="color:red">10 个随机场景</span> | <span style="color:red">10/10 全清</span> | <span style="color:red">4794 ~ 7522 秒</span> |
| <span style="color:red">同种子第一套对照</span> | <span style="color:red">10/10 全清</span> | <span style="color:red">4301 ~ 7125 秒</span> |

<span style="color:red">两套离线虚拟时间互有胜负（均值 6547 vs 5962 秒，差距约 10%）：本方案在源多（16 个）的场景 2 局更优，在源少（10~11 个）的场景因补漏阶段须扫完全部 13 个航点而稍慢；真实演练见 13.2。</span>

### 13.2 真实模拟器演练

<span style="color:red">【补充】2026-09-12 连接真实模拟器演练 1 局（队号 202610061109）：**15/15 全部清除**，虚拟时间 7272.3 秒，程序运行时间（现实）1.2 秒，平均定位清除时间 484.8 秒，无未解决频道。</span>

<span style="color:red">过程要点：</span>

- <span style="color:red">开场约 6 次 `/enter` 连接被模拟器重置（倒计时未结束），轮询重试自动渡过；</span>
- <span style="color:red">主动搜索按设计工作：除原点扫描外共执行 5 次信息收益扫描，在清除途中顺路覆盖"尚未排除的候选区域"（如位置 (820,535)、(1136,-1231) 等），其中第 10 迭代的扫描直接发现频道 8、16 两个新源；</span>
- <span style="color:red">频道 11、20 各经 4 次观测（2 次清除失败 → 原地补测修正 → 成功）、频道 1、14 各 3 次观测，失败修正机制与负信息排除按设计工作；</span>
- <span style="color:red">13 个航点全部访问完毕后，5 个频道候选区域已全部排除，判定无源（empty），确认无漏源；</span>
- <span style="color:red">平均定位清除时间 484.8 秒优于第一套演练基线（577.5 / 572.7 秒，均值 575.1 秒），验证了价值函数搜索对移动耗时的压缩效果。</span>

<span style="color:red">日志文件：robot_model3_log1.txt（每次运行自动新建编号日志，不覆盖历史）。</span>
