# 附件2：模拟器通信接口说明及编程指南

## 1. 基本设定

### 1.1 目标区域与坐标系

测试在二维虚拟目标区域内进行：

- 目标区域是半径 1800 米的圆形区域；
- 所有干扰源都位于目标区域内；
- 圆心为坐标原点 `(0, 0)`；
- `x` 轴正方向为东；
- `y` 轴正方向为北；
- 坐标单位为米。

机器狗可以提交目标区域外的位置，模拟器仍会根据该位置计算移动耗时、检测结果和清除结果。但每个坐标分量必须是有限数值，且绝对值不超过 2,000,000 米。

如果坐标为 `NaN`、正无穷、负无穷或超出范围，模拟器返回 HTTP 400，请求不执行，也不会推进位置和虚拟时间。

### 1.2 角度约定

所有方位角遵循以下约定：

- 正东方向为 `0°`；
- 逆时针方向为正；
- 正北方向为 `90°`；
- 正西方向为 `180°`；
- 正南方向为 `270°`；
- 角度范围为 `[0°, 360°)`。

`/measure` 返回的 `svd_deg` 表示干扰源的示向度，单位为度。

### 1.3 频道

- 频道用整数表示；
- 有效频道范围为 `1` 至 `20`；
- 每个频道最多对应一个干扰源；
- 一次测试中的干扰源总数为 10 至 16 个；
- 具体干扰源数量不会通过接口直接返回。

`/measure` 的 `channel` 表示测向机要检测的频道；`/clear` 的 `channel` 表示要尝试清除的干扰源频道。

特别注意：`/clear` 的 `channel` 不会改变测向机当前频道，也不会产生频道切换耗时。

### 1.4 初始状态

机器狗成功调用 `/enter` 后：

- 初始位置为 `(0, 0)`；
- 测向机初始频道为 `1`；
- 开始计算程序运行时间；
- `/enter` 本身不推进虚拟时钟。

每次测试只允许一个与当前登录参赛队号一致的机器狗进入。机器狗必须逐次发送动作，收到完整响应后才能发送下一个动作。每次检测的 5 秒只增加虚拟时间，现实中不需要等待 5 秒。

### 1.5 其他信息

默认服务地址：

```text
http://127.0.0.1:2026
```

模拟器只监听本机回环地址，其他电脑不能直接访问。非测试期间、5 秒倒计时期间和测试结束后，机器狗接口未开放，连接可能直接失败。

## 2. 关键物理规则

### 2.1 有效接收半径

每个干扰源都有一个有效接收半径，范围在 1000 至 1500 米之间，且不同干扰源可能不同。

当机器狗与干扰源的距离超过该干扰源的有效接收半径时，机器狗接收不到该频道信号。具体半径不会通过接口返回。

### 2.2 全向与定向干扰源

全向干扰源：只要距离不超过有效接收半径，就可以检测到信号。

定向干扰源：必须同时满足以下条件才能检测到信号：

1. 距离不超过有效接收半径；
2. 检测点位于干扰源的有效覆盖角度范围内。

定向方向未知，覆盖范围为定向方向两侧各 90°，包含边界。

因此，`/measure` 返回 `no_signal` 时，只能说明当前位置接收不到该频道信号。可能原因包括：

- 该频道没有未清除干扰源；
- 距离超过有效接收半径；
- 该频道的干扰源是定向干扰源，但当前位置不在覆盖范围内。

### 2.3 示向度误差

`/measure` 返回的 `svd_deg` 是检测点处测得的示向度，不是干扰源精确的真实方位。

示向度与干扰源最大场强方向的真实方位角之间存在误差，误差范围以题目正文规定为准，即约为 `±1°`。返回值保留两位小数，并按 `[0°, 360°)` 归一化。

程序不能把一次 `svd_deg` 当作完全精确的真实方向。

### 2.4 近距离阈值与清除半径

- 近距离阈值为 5 米；
- 清除半径为 20 米。

当检测点与目标距离不超过 5 米，且位于干扰源有效覆盖范围内时，信号过强，`/measure` 返回 `near`，不返回 `svd_deg`。

当清除位置与目标距离小于等于 20 米时，`/clear` 可以成功。清除是否成功只与距离有关，与定向干扰源的朝向无关。

## 3. 四条 HTTP 指令

机器狗只需要调用以下四条接口：

| 指令 | 含义 | 是否推进虚拟时钟 |
|---|---|---|
| `POST /enter` | 进入目标区域，开始本次运行 | 否 |
| `POST /measure` | 移动到指定位置，对指定频道进行检测 | 是 |
| `POST /clear` | 移动到指定位置，尝试清除指定频道的干扰源 | 是 |
| `POST /exit` | 退出目标区域，结束本次运行 | 否 |

移动和切换频道没有单独接口：

- 移动由 `/measure` 或 `/clear` 中的 `position` 体现；
- 切换频道只由 `/measure` 中的 `channel` 体现。

## 4. 虚拟时间计算

### 4.1 虚拟时钟推进规则

只有响应中 `accepted=true` 的请求才会生效。

- `/enter`：接受后不推进虚拟时钟；
- `/exit`：接受后不推进虚拟时钟；
- `/measure`：接受后推进虚拟时钟；
- `/clear`：接受后推进虚拟时钟；
- `accepted=false`：请求不生效，虚拟时钟不推进。

`accepted=false` 的响应中 `virtual_time_s` 为 0，这个 0 不是当前虚拟时刻。记录虚拟时刻时，应使用最近一次 `accepted=true` 响应中的值。

`virtual_time_s` 是 JSON number，可能带小数，不能强制按整数解析。模拟器内部按微秒累计，响应最多保留 6 位小数。

### 4.2 移动耗时

模拟器记录机器狗上一次合法动作的位置。一次合法 `/measure` 或 `/clear` 的移动耗时为：

$$
t_{move}=\frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{5}
$$

默认移动速度为 `5 m/s`。

例如，从 `(0, 0)` 移动到 `(300, 400)`，直线距离为 500 米，移动耗时为：

$$
500/5=100\text{ 秒}
$$

### 4.3 检测耗时

一次合法 `/measure` 的总耗时为：

$$
t_{measure}=t_{move}+t_{channel}+5
$$

其中：

- 检测动作耗时为 5 秒；
- 如果本次合法 `/measure` 的频道与当前测向机频道不同，切换频道耗时为 1 秒；
- 如果频道相同，切换频道耗时为 0 秒；
- 合法 `/measure` 完成后，测向机当前频道更新为本次检测频道。

### 4.4 清除耗时

一次合法 `/clear` 的总耗时为：

- 未发现可清除目标：移动耗时 + 3 秒；
- 成功清除目标：移动耗时 + 5 秒。

`/clear` 不产生频道切换耗时，也不会改变测向机当前频道。

### 4.5 现实时间和虚拟时间

用户确认开始后，模拟器先准备数据。准备完成后显示 5 秒倒计时，倒计时结束时：

- 开启 25 分钟测试窗口；
- 开放机器狗接口。

机器狗成功调用 `/enter` 后，开始计算最长 20 分钟的程序运行时间。实际截止时刻取以下两个时刻中较早者：

- 25 分钟测试窗口结束；
- `/enter` 成功后经过 20 分钟。

`/enter` 成功响应中的 `remaining_real_duration_s` 表示本局从进入开始实际剩余的完整现实时间，范围为 0 至 1200 秒。程序不能固定假设每局都有 1200 秒。

虚拟世界限时为 360000 秒。达到现实时间或虚拟时间截止条件后，模拟器结束测试并关闭接口。截止前已经完整到达并登记的唯一动作允许完成，截止时刻或之后才完整到达的请求不再执行，连接可能直接关闭。

## 5. 通信格式总规则

### 5.1 请求格式

四条指令都使用 HTTP + JSON：

- HTTP 方法必须为 `POST`；
- `Content-Type` 必须为 `application/json`，可带 `charset=utf-8`；
- `Content-Encoding` 可以省略或为 `identity`；
- 请求体必须是无 BOM 的 UTF-8 JSON 对象；
- JSON 不能有重复键；
- JSON 嵌套不能超过 16 层；
- 请求体最大 65536 字节；
- 路径必须精确为 `/enter`、`/measure`、`/clear` 或 `/exit`，不能有尾随斜线和查询参数。

公共字段要求：

- `arena_id` 必须是 ASCII 字符串 `"default"`；
- `robot_id` 必须逐字节等于当前登录参赛队号；
- `request_id` 是当前测试会话内的幂等键；
- `robot_id` 和 `request_id` 不能包含控制字符或不可见格式字符；
- `channel` 必须是 1 至 20 的整数。

未声明的 JSON 字段不会被忽略，会返回 HTTP 200 且 `accepted=false`。

### 5.2 响应格式

所有 JSON 业务响应至少包含：

| 字段 | 类型 | 含义 |
|---|---|---|
| `accepted` | boolean | 模拟器是否接受本次请求 |
| `real_timestamp_ms` | number | 模拟器响应时的现实时间戳，单位为毫秒 |
| `virtual_time_s` | number | 当前虚拟时钟，单位为秒 |

当 `accepted=false` 时，通常只包含上述三个字段，不包含检测结果、清除结果或退出原因。

### 5.3 HTTP 状态和业务接受状态

| HTTP 状态 | 含义 |
|---:|---|
| 200，`accepted=true` | 动作已执行 |
| 200，`accepted=false` | JSON 结构合法，但测试状态拒绝动作，或存在未知字段、标识不匹配等问题 |
| 400 | JSON 语法、重复键、缺失字段、字段类型、标识符格式、频道或坐标范围错误 |
| 404 | 路径未知或路径不精确 |
| 405 | 已知路径使用了非 POST 方法 |
| 409 | 同一 `request_id` 对应不同动作，或并发发送了不同动作 |
| 413 | 请求体超过 65536 字节 |
| 415 | `Content-Type` 或 `Content-Encoding` 不受支持 |
| 429 | 连接/无效流量保护触发，或幂等记录达到上限 |
| 500 | 模拟器内部错误 |

程序必须同时检查 HTTP 状态和 `accepted`，不能只判断其中一个。

### 5.4 幂等和重试

每个新动作必须使用新的 `request_id`。只有网络超时或连接中断，重试完全相同动作时，才复用原请求内容和原 `request_id`。

- 同一 ID、同一内容：返回第一次的完整响应，不会重复移动、检测、清除、退出或推进时间；
- 同一 ID 但修改路径、位置或频道：返回 HTTP 409；
- 结构错误、未知字段、`arena_id` 或 `robot_id` 不匹配：不占用该 ID，修正后可以复用；
- 不同的新动作不得并发发送。

倒计时未结束、接口未开放、测试结束或模拟器无法继续服务时，连接可能直接关闭而没有 JSON 响应，程序必须处理这种情况。

## 6. `POST /enter`：进入目标区域

### 6.1 请求字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `arena_id` | string | 是 | 必须为 `"default"` |
| `robot_id` | string | 是 | 机器狗标识，应填写参赛队号 |
| `request_id` | string | 是 | 请求标识，不能为空 |

请求示例：

```json
{
  "arena_id": "default",
  "robot_id": "<参赛队号>",
  "request_id": "enter-1"
}
```

### 6.2 成功响应字段

| 字段 | 说明 |
|---|---|
| `accepted` | `true` |
| `real_timestamp_ms` | 现实时间戳，单位为毫秒 |
| `virtual_time_s` | 进入后的虚拟时刻，`/enter` 不推进虚拟时钟 |
| `max_virtual_duration_s` | 虚拟世界限时，默认 360000 |
| `max_real_duration_s` | 现实世界限时，默认 1200 |
| `remaining_real_duration_s` | 本局从进入开始实际剩余的完整现实时间 |

示例：

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 0,
  "max_virtual_duration_s": 360000,
  "max_real_duration_s": 1200,
  "remaining_real_duration_s": 1200
}
```

### 6.3 常见未执行原因

`accepted=false` 的常见原因：

- `arena_id` 不是 `default`；
- `robot_id` 与当前登录参赛队号不一致；
- 请求包含未声明字段；
- 重复调用 `/enter`。

HTTP 400 的常见原因：请求体非法、缺少必要字段、标识为空或格式不合法。测试结束或接口未开放时，连接可能直接失败。

## 7. `POST /measure`：检测

### 7.1 请求字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `arena_id` | string | 是 | 必须为 `"default"` |
| `robot_id` | string | 是 | 参赛队号 |
| `request_id` | string | 是 | 请求标识 |
| `position` | object | 是 | 检测位置 |
| `position.x` | number | 是 | 有限坐标，绝对值不超过 2000000 |
| `position.y` | number | 是 | 有限坐标，绝对值不超过 2000000 |
| `channel` | number | 是 | 1 至 20 的整数 |

请求示例：

```json
{
  "arena_id": "default",
  "robot_id": "<参赛队号>",
  "request_id": "measure-1",
  "position": {"x": 300, "y": 400},
  "channel": 1
}
```

### 7.2 成功响应字段

| 字段 | 说明 |
|---|---|
| `accepted` | 是否接受 |
| `real_timestamp_ms` | 现实时间戳 |
| `virtual_time_s` | 本次检测完成后的虚拟时刻 |
| `measure_result` | `no_signal`、`near` 或 `direction` |
| `svd_deg` | 只有 `measure_result="direction"` 时返回，表示示向度 |

### 7.3 三种检测结果

#### 无信号

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 105,
  "measure_result": "no_signal"
}
```

可能原因是频道没有未清除干扰源、距离太远，或定向干扰源没有覆盖当前位置。

#### 距离过近

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 105,
  "measure_result": "near"
}
```

说明已经检测到目标，距离不超过 5 米，但由于信号过强无法返回示向度。

#### 返回示向度

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 105,
  "measure_result": "direction",
  "svd_deg": 123.45
}
```

说明检测到信号，并返回从检测点指向干扰源的方位角，但该角度包含误差。

## 8. `POST /clear`：清除

### 8.1 请求字段

请求结构与 `/measure` 类似，`channel` 表示要尝试清除的目标频道：

```json
{
  "arena_id": "default",
  "robot_id": "<参赛队号>",
  "request_id": "clear-1",
  "position": {"x": 300, "y": 0},
  "channel": 3
}
```

### 8.2 清除规则

- 清除半径为 20 米；
- 指定频道的未清除干扰源距离 `position` 小于等于 20 米时，清除成功；
- 全向和定向干扰源都适用；
- 定向干扰源的清除不受发射方向限制；
- 目标不存在、已经清除或距离超过 20 米时，均视为未发现目标；
- 同一个干扰源只能清除一次；
- `/clear` 不切换测向机当前频道。

### 8.3 成功响应字段

| 字段 | 说明 |
|---|---|
| `accepted` | 是否接受 |
| `real_timestamp_ms` | 现实时间戳 |
| `virtual_time_s` | 本次清除完成后的虚拟时刻 |
| `clear_result` | `success` 或 `no_target_in_range` |

成功清除示例：

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 196,
  "clear_result": "success"
}
```

未发现目标示例：

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 194,
  "clear_result": "no_target_in_range"
}
```

## 9. `POST /exit`：退出目标区域

### 9.1 请求字段

```json
{
  "arena_id": "default",
  "robot_id": "<参赛队号>",
  "request_id": "exit-1"
}
```

### 9.2 成功响应字段

| 字段 | 说明 |
|---|---|
| `accepted` | `true` |
| `real_timestamp_ms` | 现实时间戳 |
| `virtual_time_s` | 退出时的虚拟时刻，`/exit` 不推进虚拟时间 |
| `exit_reason` | 退出原因，主动退出时为 `user_exit` |

示例：

```json
{
  "accepted": true,
  "real_timestamp_ms": 1760000000000,
  "virtual_time_s": 199,
  "exit_reason": "user_exit"
}
```

测试因超时或手工中止结束时，接口会直接关闭，不能事后通过 `/exit` 查询原因。

## 10. 完整计时示例

本示例只说明指令含义和虚拟耗时，不代表具体搜索策略。

| 步骤 | 指令 | 位置 | 频道含义 | 移动距离 | 移动耗时 | 切换耗时 | 动作耗时 | 本步总耗时 | 虚拟时刻 |
|---:|---|---|---|---:|---:|---:|---:|---:|---:|
| 1 | `/enter` | - | - | 0 | 0 | 0 | 0 | 0 | 0 |
| 2 | `/measure` | `(300,400)` | 检测频道 1 | 500 米 | 100 秒 | 0 | 5 秒 | 105 秒 | 105 |
| 3 | `/measure` | `(300,400)` | 检测频道 2 | 0 米 | 0 | 1 秒 | 5 秒 | 6 秒 | 111 |
| 4 | `/clear` | `(300,0)` | 清除频道 3 | 400 米 | 80 秒 | 0 | 3 秒 | 83 秒 | 194 |
| 5 | `/measure` | `(300,0)` | 检测频道 2 | 0 米 | 0 | 0 | 5 秒 | 5 秒 | 199 |
| 6 | `/exit` | - | - | 0 | 0 | 0 | 0 | 0 | 199 |

关键点：

- 从 `(0,0)` 到 `(300,400)` 距离 500 米，移动耗时 100 秒；
- 第 3 步频道从 1 变为 2，增加 1 秒切换耗时；
- 第 4 步是 `/clear`，频道 3 不会改变测向机频道；
- 第 5 步继续检测频道 2，因此不需要切换频道。

## 11. 示例程序的基本流程

无论使用 Python、Matlab 还是其他语言，程序流程都应遵循：

1. 设置模拟器地址、参赛队号和请求函数；
2. 调用 `/enter`；
3. 检查 HTTP 状态和 `accepted`；
4. 读取 `remaining_real_duration_s`；
5. 按策略逐次调用 `/measure` 和 `/clear`；
6. 根据 `measure_result` 读取 `svd_deg`；
7. 根据 `clear_result` 判断是否清除成功；
8. 完成任务后调用 `/exit`。

只有在 `measure_result="direction"` 时读取 `svd_deg`。网络故障重试时必须复用同一个请求体和 `request_id`。

## 12. 编程注意事项

- 先在线登录模拟器，并等待接口就绪；
- 使用当前登录参赛队号作为 `robot_id`；
- 每个新动作使用新的 `request_id`；
- 只有重试完全相同动作时才复用原 ID；
- 逐次等待响应，不得并发发送不同动作；
- 同时检查 HTTP 状态和 `accepted`；
- 不要把 `accepted=false` 响应中的 `virtual_time_s=0` 当作当前虚拟时刻；
- 使用 `/enter` 返回的 `remaining_real_duration_s` 控制现实运行时间；
- 每次检测增加的 5 秒是虚拟耗时，现实中不需要等待；
- 只有 `direction` 结果才读取 `svd_deg`；
- `/clear` 不切换测向机频道；
- `no_signal` 不等于附近一定没有干扰源；
- 测试结束后接口会关闭，不要尝试调用 `/exit` 查询原因；
- 程序判断 JSON 时使用实际字符串：`direction`、`near`、`no_signal`、`success`、`no_target_in_range` 和 `user_exit`；
- 程序应自行记录测试过程中的指令和响应；
- 避免异常高频循环，确保正式日志文件不超过 2 MB；
- 接口未开放或测试结束时，连接可能直接失败，没有 JSON 响应。
