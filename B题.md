# 2026 年高教社杯全国大学生数学建模竞赛题目

> 请先阅读《全国大学生数学建模竞赛论文格式规范》。

# B题：无线电干扰源的快速自动定位与清除

## 一、题目背景

无线电干扰源的定位与清除是无线电频谱管理的重要任务。无线电干扰源的定位通常分为三个阶段：

1. 初步监测；
2. 区域性排查；
3. 精准定位。

前两个阶段使用固定监测站、移动监测车或无人机等设备进行大范围信号采集与分析，确定干扰源目标的分布区域。然而，受环境遮挡、低功率干扰源等因素影响，每个干扰源的精准定位最终仍需要技术人员进入目标分布区域内完成。

为了尽快消除干扰造成的不良影响，任务完成时间越短越好。在精准定位阶段，技术人员携带便携式无线电方位监测测向机，通过观察和分析信号场强变化、方向特征，结合经验逐步定位干扰源，最终确定干扰源位置并将其清除。

某公司拟研发搭载于机器狗上的干扰源自动定位清除系统，使机器狗能够在危险环境中代替技术人员开展定位和清除工作，提高任务效率。干扰源特征见[附录1](#附录1-干扰源的特征)，检测原理和工作方式见[附录2](#附录2-测向机等设备的原理和工作方式)。

干扰源目标分布区域已确定为半径为 1800 米的圆形区域。以该圆域中心为坐标原点，正东方向为 $x$ 轴正向，正北方向为 $y$ 轴正向，坐标单位为米。目标区域中的干扰源数量未知。

请建立数学模型解决以下问题。

---

## 二、问题

### 问题1：交会定位区域直径

已知若干个检测点坐标及某干扰源关于这些检测点的示向度，请给出采用交会定位法（见附录2）计算多边形定位区域直径的算法。

定位区域直径是指区域内任意两点之间距离的最大值。

进一步回答：以定位区域直径为直径的圆能否覆盖该定位区域？

### 问题2：第二检测点的选择

已知某全向干扰源在一个检测点处测得的示向度，请给出第二个检测点的选择策略，以获得关于该干扰源较好的定位效果，并给出第二个检测点的候选区域。

### 问题3：全向干扰源的自动定位与清除

已知目标区域中的干扰源均为全向干扰源，总数在 10 至 16 个之间，但具体个数未知。

现派出一只机器狗从原点出发，测向机初始频道为 1，对目标区域中的干扰源进行自动定位并清除。要求在确保所有干扰源被清除的前提下，完成任务的时间越短越好。

请制定机器狗自动搜索、定位及清除的策略，并设计相应算法。

请通过模拟器进行演练测试（见[附录3](#附录3-问题3测试说明)），计算以下统计值，以检验策略的有效性：

#### 被清除干扰源的个数

统计机器狗成功清除的干扰源数量。

#### 被清除干扰源个数的比例

$$
\text{被清除干扰源个数的比例}
=
\frac{\text{被清除干扰源的个数}}{\text{干扰源总数}}
$$

#### 平均定位清除时间

$$
\text{平均定位清除时间}
=
\frac{\text{定位清除总时间}}{\text{被清除干扰源的个数}}
$$

其中，定位清除总时间包括：

- 机器狗移动时间；
- 频道切换时间；
- 检测时间；
- 光学精确定位时间；
- 清除时间等。

在充分演练测试后，再进行三次正式测试。请在论文中按照表 1 的格式给出每次正式测试的结果。在模拟器中导出三次正式测试的日志文件，不要修改文件名，并放入支撑材料中。

#### 表1：问题3正式测试结果

| 测试 | 测试案例编码 | 清除干扰源个数 | 平均定位清除时间 | 程序运行时间 |
|---|---|---:|---:|---:|
| 测试1 |  |  |  |  |
| 测试2 |  |  |  |  |
| 测试3 |  |  |  |  |

### 问题4：含定向干扰源的自动定位与清除

已知目标区域中既有全向干扰源，又有定向干扰源，总数仍为 10 至 16 个，但具体个数未知；定向干扰源的个数及定向方向也未知，其他条件与问题3相同。

请在此条件下给出检测与清除策略及算法，并通过模拟器进行演练测试（见[附录4](#附录4-问题4测试说明)），检验其有效性。

在充分演练测试后，再进行三次正式测试。请在论文中按照表 1 的格式给出每次正式测试的结果。在模拟器中导出三次正式测试的日志文件，放入支撑材料中。

---

# 附录1：干扰源的特征

1. 每个干扰源的频道互不相同，且不随时间发生变化。干扰源频道位于常见的 20 个频道中，根据各频道中心频率的不同依次编号为 1 至 20。
2. 不同干扰源的信号互不影响。
3. 按信号覆盖角度范围划分，干扰源分为全向干扰源与定向干扰源两类：
   - 全向干扰源无特定发射指向，有效覆盖角度范围为 $360^\circ$；
   - 定向干扰源具有特定的发射方向，简称定向方向；
   - 定向干扰源的有效覆盖角度范围为定向方向两侧各 $90^\circ$，含边界；
   - 在有效覆盖角度范围之外没有信号辐射。
4. 全向干扰源和定向干扰源在其有效覆盖角度范围内均匀辐射，信号场强仅随距离衰减，与角度无关。

---

# 附录2：测向机等设备的原理和工作方式

## 1. 示向度与测量误差

测向机通过检测干扰源的场强变化，推断干扰源相对于检测点的位置方向。

当检测点位置不变时，接收天线指向干扰源时，检测到的场强最大。将测向机在检测点接收到的最大场强方向的方位角称为测向机在该检测点处测得的示向度，简称示向度。

方位角定义如下：

- 从 $x$ 轴正向逆时针旋转至检测点指向干扰源的向量方向；
- 单位为度；
- 取值范围为 $[0^\circ,360^\circ)$。

由于环境影响及测向精度限制，测向机读数与干扰源最大场强方向的真实方位角之间存在误差。

在一段时间内，同一地点的电磁环境干扰是固定的，因此重复检测不会改变该地点的检测误差；只有在不同地点、不同电磁环境下，误差才呈现统计规律。

从目标区域全局来看，所有误差均在 $[-1^\circ,1^\circ]$ 范围内。

> 原题图1：干扰源 $G$ 关于检测点 $S$ 的示向度示意图。

## 2. 有效接收距离

测向机的灵敏度是其能够有效接收到的信号的最小场强。

本题中各个干扰源的发射功率及测向机灵敏度均已确定。每个干扰源都有一个最大可被有效接收距离，称为有效接收半径，其范围为 1000 米至 1500 米。

## 3. 交会定位法

对多个干扰源进行定位时，通常使用交会定位法。

原题图2展示了两个检测点 $S_1$、$S_2$ 对同一干扰源 $G$ 进行交会定位的示意图：

- 两条实线方向分别表示在 $S_1$、$S_2$ 处测得的 $G$ 的示向度方向；
- 每条实线与相邻两条虚线的夹角均为 $1^\circ$；
- 红色四边形称为定位区域。

> 原题图2：交会定位法示意图。

## 4. 频道切换

测向机不能同时检测多个频道的场强。每次变换检测频道时，需要先将当前频道切换到拟检测频道。

任意两个频道之间的切换时间为 1 秒。

## 5. 检测过程

移动过程中无法进行有效检测，机器狗必须停下来进行检测。

机器狗在某一点进行检测时，需要：

1. 停止移动；
2. 切换到拟检测频道；
3. 不断调整天线朝向；
4. 获得该频道干扰源的稳定示向度，或确认无信号。

调整天线并获得稳定读数或确认无信号的时间为 5 秒。

## 6. 机器狗移动

机器狗沿直线行进，移动速度为 5 米/秒。

## 7. 平面假设

忽略机器狗所携带测向机与干扰源之间的高度差异，认为测向机与所有干扰源位于同一平面上。

## 8. 光学定位与清除

由于环境干扰因素及仪器参数限制，机器狗携带的简易光学探测仪只有在距离干扰源不超过 20 米时，才能精确定位干扰源并启动激光枪清除。

- 启用光学探测仪精确定位的时间为 3 秒；
- 启用激光枪清除干扰源的时间为 2 秒；
- 光学精确定位和清除是否成功，仅与清除点和干扰源之间的距离有关；
- 清除是否成功与定向干扰源的信号覆盖方向无关。

## 9. 近距离检测

当检测点与干扰源的距离不超过 5 米，且位于干扰源信号有效覆盖角度范围内时，由于信号太强，测向机无法获得示向度信息。

此时可以直接进行光学精确定位并清除干扰源。

---

# 附录3：问题3测试说明

请先下载无线电干扰源环境模拟器，简称模拟器，用于测试。

机器狗程序通过 HTTP + JSON 接口与模拟器通信，发送检测、清除等指令，并获取模拟器反馈的检测结果或清除结果，按照所设计的算法完成干扰源定位与清除。通信协议、具体测试过程和要求见附件1、附件2。

## 1. 测试案例数据

每个测试案例包括以下数据：

- 目标区域中的干扰源数量；
- 频道编号；
- 干扰源位置；
- 有效接收半径等。

上述数据均无法直接读取。

## 2. 演练测试与正式测试

测试分为“演练测试”和“正式测试”两个模块。

演练测试用于检验策略和算法的效果，其测试环境及操作流程与正式测试完全一致，但演练测试没有测试次数限制，并且测试结束时会给出干扰源数量。

正式测试每次随机生成一个案例，共进行三次正式测试。

## 3. 测试时间流程

用户确认开始测试后，模拟器会：

1. 准备案例数据；
2. 数据准备完成后显示 5 秒倒计时；
3. 倒计时结束时开启 25 分钟测试窗口；
4. 同时开放机器狗接口。

“中止测试”按钮仅用于手工结束测试，但不能取消本次测试，同样会占用一次测试机会。

机器狗应在界面显示接口已经就绪后调用 `/enter` 进入目标区域。

成功调用 `/enter` 后：

- 开始计算机器狗的虚拟时间；
- 开始计算程序运行时间；
- 程序运行时间最长为 20 分钟。

测试同时受 25 分钟测试窗口限制，以较早到达的截止条件为准。因此，在 25 分钟窗口开始后 5 分钟内成功调用 `/enter`，可以使用完整的 20 分钟；晚于 5 分钟进入时，实际可用时间不足 20 分钟。

`/enter` 响应会返回本次实际剩余的程序运行时间。

机器狗每次测试的初始状态为：

- 初始位置：$(0,0)$；
- 初始频道：1。

机器狗在虚拟世界中的活动时长通常不构成约束，但为防止程序死循环，活动时长上限为 100 小时。

机器狗位置通常也不构成约束，但为避免数值异常，提交给接口的每个坐标分量绝对值不得超过 2,000,000 米。

任务完成后，机器狗应调用 `/exit` 主动结束测试。

可能的测试结束原因包括：

- 主动退出；
- 手工中止；
- 25 分钟窗口超时；
- `/enter` 后程序运行超时；
- 虚拟时间超时；
- 技术异常。

通常，`/enter` 指令与测试结束时刻之间的时长被视为该次测试的程序运行时间。

## 4. 日志文件

每次正式测试结束后，模拟器会生成加密行为日志并自动加入上传队列。选手还需要从模拟器导出对应日志，放入支撑材料中。

演练测试和正式测试都要求模拟器全程保持网络连接，并在开始测试前完成在线登录和服务器时间校验。

北京时间 2026 年 9 月 13 日 17:30 之后，模拟器不能启动新的演练测试或正式测试。

在此之前已经开始的测试不受影响，可以正常完成；只要行为日志已经进入上传队列，模拟器就会继续自动上传，不受该时间限制。

为避免竞赛后期网络拥堵，建议尽早进行正式测试，建议在北京时间 2026 年 9 月 13 日 15:30 之前完成全部正式测试。

---

# 附录4：问题4测试说明

测试案例包括以下数据：

- 目标区域中的干扰源数量；
- 频道编号；
- 干扰源位置；
- 有效接收半径；
- 定向方向等。

其中，全向干扰源的定向方向为空。所有数据均无法直接读取。

测试同样分为演练测试和正式测试两个模块：

- 演练测试的环境及操作流程与正式测试完全一致；
- 演练测试没有测试次数限制；
- 演练测试结束时会给出干扰源数量；
- 正式测试每次随机生成一个案例，共进行三次正式测试。

正式测试中的初始状态、启动流程、时间限制、联网要求、行为日志和截止时间等，与问题3正式测试相同。
