# 问题4：全向与定向混合干扰源的检测与清除策略

## 一、问题分析

### 1.1 定向干扰源的特征

与问题3（全向干扰源）相比，问题4的目标区域中同时存在全向干扰源和定向干扰源：

- **全向干扰源**：有效覆盖角度 $360^\circ$，在有效接收半径内任何方向均可检测到信号；
- **定向干扰源**：有效覆盖角度为定向方向两侧各 $90^\circ$（即 $180^\circ$ 扇形区域），在覆盖范围外无信号辐射；
- 定向干扰源的个数及定向方向均未知；
- 清除操作不受定向角度限制（光学精确定位及清除仅与距离有关，距离≤20米即可清除）。

### 1.2 核心挑战

1. **类型不可知**：无法直接判断一个频道是全向还是定向，也不知道定向方向；
2. **信号缺失歧义**：在某点检测不到信号（`no_signal`）可能有三种原因——该频道无干扰源、超出有效接收半径、定向干扰源但当前位置在覆盖角度外；
3. **覆盖盲区**：定向干扰源有 $180^\circ$ 的无信号区域，单一方向的搜索可能完全检测不到；
4. **定位需在覆盖区内**：交会定位需要至少2个在覆盖范围内的检测点获得示向度。

<span style="color:red">【补充·固有局限】若定向干扰源的覆盖扇形完全朝向场外（场内所有检测点均落在其盲区内），则该源在所有检测点都表现为"无信号"，与"该频道无干扰源"不可区分，任何被动搜索策略都无法发现；若已访问的检测点恰好全部落在某源的盲区内，该频道会被暂时误判为无信号。只能依靠检测点从多方向、多距离包围（13点布局）降低其发生概率，无法完全消除——这是该问题的固有极限，如实列出。</span>

### 1.3 关键观察

- 定向干扰源的覆盖范围是 $180^\circ$ 扇形，面积占全向的一半，但只要检测点落在覆盖扇形内就能获得示向度；
- 全向干扰源在接收半径内任何方向都有信号，定向干扰源只有一半方向有信号——**通过多点检测的信号/无信号模式可以区分类型**；
- 清除不受定向影响，定位后的清除流程与问题3完全相同；
- 定向干扰源的示向度误差模型与全向相同（$\pm 1^\circ$），交会定位算法（问题1）可直接复用。

## 二、干扰源类型识别与定向方向估计

### 2.1 类型判据

设某频道在 $k$ 个不同检测点进行了检测，检测点到估计干扰源位置的距离均在有效接收半径内。定义：

- $N_{sig}$：检测到信号（`direction` 或 `near`）的点数；
- $N_{nosig}$：未检测到信号（`no_signal`）的点数；
- $N = N_{sig} + N_{nosig}$：总检测点数。

**判据**：

| 检测模式 | 判定 | 置信度 |
|----------|------|--------|
| $N_{nosig} \ge 1$ 且 $N_{sig} \ge 1$ | 定向干扰源 | 高（全向不可能在接收半径内出现无信号） |
| $N_{nosig} = 0$，$N_{sig} \ge 3$（方向分散） | 可能为全向 | 中（也可能是定向但所有点恰好都在覆盖区内） |
| $N_{sig} = 0$ | 无法判断 | 低（可能无干扰源、超距、或全在覆盖盲区） |

**核心结论**：只要在接收半径内同时观测到"有信号"和"无信号"，即可确定该频道为定向干扰源。

<span style="color:red">【补充·迭代判定】上述判据依赖"检测点到干扰源位置的距离"，但阶段1粗扫结束时尚无可靠的定位估计。实际实现采用**迭代判定**：先用已有示向度粗略估计源位置（两示向度交点；仅1个示向度时沿该射线外推约500m作近似），据此筛出"接收半径内却无信号"的检测点执行判据；此后每获得新观测（补漏定位、清除修正）都重新执行类型判定与方向估计，判定结果随定位精度提高而逐步收敛。</span>

### 2.2 定向方向估计

当判定为定向干扰源后，需要估计其定向方向。设干扰源估计位置为 $G=(x_g,y_g)$，各检测点 $P_i=(x_i,y_i)$ 相对于 $G$ 的方位角为：

$$\phi_i = \text{atan2}(y_i - y_g,\ x_i - x_g) \pmod{360^\circ}$$

定向干扰源的覆盖范围为 $[\theta_d - 90^\circ,\ \theta_d + 90^\circ]$，其中 $\theta_d$ 为定向方向。

**估计方法**：

1. 将有信号的检测点方位角集合记为 $\Phi_{sig} = \{\phi_i | \text{有信号}\}$；
2. 将无信号的检测点方位角集合记为 $\Phi_{nosig} = \{\phi_i | \text{无信号}\}$；
3. 寻找一个 $180^\circ$ 的区间 $[\theta_d - 90^\circ,\ \theta_d + 90^\circ]$，使得：
   - 所有 $\phi_i \in \Phi_{sig}$ 都落在该区间内；
   - 所有 $\phi_i \in \Phi_{nosig}$ 都落在该区间外。
4. 该区间的中心角 $\theta_d$ 即为定向方向的估计值。

**实现**：将所有方位角排序，滑动一个 $180^\circ$ 的窗口，找到包含所有 $\Phi_{sig}$ 且不包含任何 $\Phi_{nosig}$ 的窗口，窗口中心即为 $\theta_d$。

**精度**：随着检测点增多，定向方向估计精度提高。初始估计误差可能较大（$\pm 30^\circ$），但足以指导后续搜索方向。

### 2.3 全向干扰源的确认

对于 $N_{nosig}=0$ 的频道，不能立即确定为全向。需要：
- 在至少3个方位角相差较大（如间隔 $120^\circ$）的检测点检测；
- 若均有信号，则高度怀疑为全向；
- 若仍无法确认，可按全向处理（保守策略：全向假设下搜索范围更大，不会遗漏定向信号）。

## 三、搜索策略

### 3.1 设计原则

针对定向干扰源的覆盖盲区，搜索策略需要满足：

1. **多方向覆盖**：确保从多个方向接近每个可能的干扰源位置，避免全部落在定向盲区；
2. **信息增益优先**：优先移动到能最大程度区分"全向/定向"和估计定向方向的位置；
3. **复用问题3策略**：全向干扰源的处理流程与问题3相同，定向干扰源在获得示向度后定位清除流程也相同。

### 3.2 三阶段搜索架构

#### 阶段1：全局粗扫（发现频道 + 初步分类）

在目标区域内选择一组检测点，确保覆盖整个区域且从多个方向接近中心区域。

**检测点布局**：
- 中心点 $(0,0)$；
- 内圈6点：半径 $600$ 米，间隔 $60^\circ$；
- 外圈6点：半径 $1200$ 米，间隔 $60^\circ$（与内圈错开 $30^\circ$）。

共13个检测点。布局理由：
- 有效接收半径最小 $1000$ 米，内圈点（距中心600米）的接收圆可覆盖中心区域；
- 外圈点（距中心1200米）可覆盖区域边缘；
- 内外圈错开确保从不同方向接近每个位置，增加检测到定向信号的概率；
- 任意位置到最近检测点的距离不超过约 $700$ 米，配合 $1000$ 米接收半径可全覆盖。

在每个检测点对所有20个频道依次检测，记录：
- 有信号的频道及示向度；
- 无信号的频道（用于后续类型判断）。

#### 阶段2：定向识别与补充检测

全局粗扫后，对每个有信号的频道：

1. **类型判断**：根据2.1节判据判断全向/定向；
2. **定向方向估计**：若为定向，按2.2节估计定向方向；
3. **补充检测**：
   - 对于疑似全向但证据不足的频道，在方位角相差 $120^\circ$ 的位置补充检测；
   - 对于定向干扰源，在估计的覆盖区域内补充检测点，以获得更多示向度用于精确定位；
   - 对于只有1次观测的频道（全向或定向），按问题2策略选择第二个检测点。

#### 阶段3：定位清除与动态搜索

对每个已定位的干扰源：
- 移动到估计位置附近（≤20米），执行清除；
- 清除后从新位置继续检测其他未清除频道；
- 若清除失败，在附近补充检测修正位置（同问题3）。

当所有已知频道处理完后，若仍有时间，移动到未充分覆盖的区域继续搜索，重点关注：
- 之前检测中 `no_signal` 较多的频道（可能是定向干扰源，之前的点全在盲区）；
- 区域边缘未充分覆盖的方向。

### 3.3 定向干扰源的搜索增强

对于判定为定向但示向度观测不足（<2次）的频道：

1. 利用已有1次观测的检测点 $P_1$ 和示向度 $\theta_1$，估计干扰源在射线 $P_1 \to \theta_1$ 上；
2. 估计定向方向 $\theta_d$（利用无信号点的方位信息）；
3. 选择第二个检测点 $P_2$，使得：
   - $P_2$ 落在估计的覆盖扇形内（确保能检测到信号）；
   - 两条示向度线交角接近 $90^\circ$（定位精度高，问题2结论）；
   - $P_2$ 到估计干扰源位置的距离在有效接收半径内。
4. 在 $P_2$ 检测该频道，获得第二次示向度后交会定位。

## 四、定位策略

### 4.1 交会定位（复用问题1）

对于全向和定向干扰源，只要获得 $\ge 2$ 次有效示向度，即可使用问题1的半平面交算法进行交会定位：

- 每次观测（位置 + 示向度）转化为2个半平面（示向度 $\pm 1^\circ$ 扇区）；
- 所有半平面取交集得到凸多边形定位区域；
- 取质心作为估计位置，记录定位区域直径作为不确定性度量。

<span style="color:red">【修正·实现声明】实际程序采用与问题3第三套策略一致的**两示向度射线交点法**：直接求两射线交点；两射线平行、或交点落在某条射线的反方向上（源位于两测点之间）时，退化为两测点连线中点；估计位置一律限幅在场内（距原点≤1790m）。定位精度由清除阶段的修正链保证（清除失败→原地重测→横向探针重定位→定向引导探针→±15m网格兜底）。半平面交仅作为理论定位模型，不在程序中使用。</span>

### 4.2 定向干扰源定位的特殊处理

定向干扰源的示向度仅在覆盖范围内有效，但一旦获得示向度，其几何意义与全向完全相同。因此：

- **定位算法不变**：直接复用问题1算法；
- **观测筛选**：仅使用 `measure_result="direction"` 的观测，`no_signal` 的点不参与定位（但参与类型判断和定向方向估计）；
- **定位精度**：定向干扰源可能获得的观测点数较少（受覆盖范围限制），定位区域可能较大，需要更多补充检测。

### 4.3 利用定向方向约束优化定位

当定向方向 $\theta_d$ 已知时，可以额外添加约束：干扰源相对于检测点的方位必须落在覆盖扇形内。但这一约束在半平面交中已隐含体现（只有覆盖区内的点才能获得示向度），一般无需额外处理。

## 五、清除策略

清除策略与问题3完全相同，因为：

> 光学精确定位及清除操作是否成功仅与清除点与干扰源之间的距离有关，与干扰源信号有效覆盖角度范围无关。

清除流程：
1. 移动到估计位置（直接使用 `/clear` 指令移动并尝试清除，节省5秒检测时间）；
2. 若清除成功（`success`），标记该频道已清除；
3. 若清除失败（`no_target_in_range`），说明估计位置偏差>20米，触发位置修正：
   - 在当前位置重新检测该频道（若在覆盖范围内可获得新示向度）；
   - 加入观测集合重新定位；
   - 移动到新估计位置再次清除；
   - 最多重试3次。

<span style="color:red">【补充】当前位置若落在定向盲区内，原地检测将返回 `no_signal`——此时不能按"无干扰源"处理，也不能放弃修正。修正链增加一步：若该频道已判定为定向（或原地检测/横向探针均无信号后被判定为定向），利用定向方向估计 $\theta_d$ 做**方向引导探针**：从当前位置沿 $\theta_d$ 方向前进400m、垂直偏移300m后检测，主动进入覆盖扇形获取新示向度，再交会重定位。</span>

## 六、算法设计

### 6.1 主算法伪代码

```
算法：混合干扰源自动检测清除
输入：模拟器接口
输出：清除的干扰源列表、统计信息

1. /enter 进入目标区域
2. 初始化：已清除集合=∅，观测字典=∅，信号模式字典=∅
3. 阶段1：全局粗扫
   对每个预定义检测点 P（13个）：
     移动到 P
     对每个未清除频道 ch：
       检测 → 记录结果
       若有信号：观测字典[ch].append((P, 示向度))
       信号模式字典[ch].append((P, 有信号/无信号))
4. 阶段2：类型识别与补充检测
   对每个有信号的频道 ch：
     判断类型（全向/定向/未知）
     若为定向：估计定向方向 θ_d
     若观测数 < 2：
       选择第二个检测点（考虑定向覆盖范围）
       移动并检测，补充观测
5. 阶段3：定位清除循环
   循环（时间未耗尽）：
     更新所有频道的定位估计
     选择目标（已定位最近优先）
     若有目标：移动清除（失败则修正重试）
     若无目标且时间充足：移动到未覆盖区域搜索
     若全部已发现频道均已处理完毕：提前结束
6. /exit 退出
7. 输出统计结果
```

<span style="color:red">【修正】原伪代码"已清除≥16个提前结束"是按问题3演练源数（约11～16个）写死的阈值；问题4混合场景实际源数未定，不宜照搬。实现为：阶段4处理完全部已定位频道（清除成功或如实标记失败）后即退出。</span>

### 6.2 类型识别算法

```
算法：classify_source(信号模式列表, 估计位置G)
输入：[(P_i, has_signal_i), ...]，估计位置G
输出：类型（全向/定向/未知），定向方向θ_d（若定向）

1. 计算每个检测点相对于G的方位角 φ_i
2. N_sig = 有信号的点数，N_no = 无信号的点数
3. 若 N_sig ≥ 1 且 N_no ≥ 1：
     返回 "定向"，估计θ_d（滑动180°窗口法）
4. 若 N_no = 0 且 N_sig ≥ 3（方位分散）：
     返回 "全向"
5. 否则：
     返回 "未知"
```

### 6.3 定向方向估计算法

```
算法：estimate_direction(Φ_sig, Φ_nosig)
输入：有信号方位角集合，无信号方位角集合
输出：定向方向θ_d

1. 将所有方位角排序（0°-360°）
2. 对每个可能的起始角 α（步长1°）：
     区间 = [α, α+180°]（模360°）
     若 所有Φ_sig都在区间内 且 所有Φ_nosig都在区间外：
       返回 θ_d = α + 90°（模360°）
3. 若未找到精确区间，返回包含最多Φ_sig且最少Φ_nosig的区间中心
```

## 七、与问题3策略的对比

| 维度 | 问题3（全向） | 问题4（混合） |
|------|--------------|--------------|
| 搜索检测点 | 6方向×2距离=12点+中心 | 6方向×2距离（内外圈错开）=12点+中心，强调多方向覆盖 |
| 类型识别 | 无需 | 必须区分全向/定向，估计定向方向 |
| no_signal含义 | 无干扰源或超距 | 无干扰源/超距/**在定向盲区**（三种可能） |
| 第二检测点选择 | 问题2策略（交角≈90°） | 问题2策略 + **必须落在定向覆盖区内** |
| 定位算法 | 半平面交（问题1） | 相同 |
| 清除算法 | 距离≤20米即可 | 相同（不受定向影响） |
| 补充检测 | 仅在观测不足时 | 观测不足 + **类型证据不足 + 定向方向估计** |
| 时间开销 | 较低 | 较高（需要更多检测点用于类型识别） |

## 八、时间分析与优化

### 8.1 额外时间开销

问题4相比问题3的额外时间主要来自：
- **全局粗扫**：13个检测点×20频道×6秒 ≈ 1560秒（如果全部扫完）

<span style="color:red">【修正】原稿"这超过了1200秒限制"混淆了两种时钟：1200秒是**现实时间**上限（正式测试的现实时长限制），虚拟时间上限为360000秒，两者口径不同；问题3演练实测每局虚拟时间5000～7500秒、现实时间仅数十秒。因此13点全扫约1560～2200虚拟秒完全在时限之内，不构成瓶颈，无需为此削减检测点。</span>

### 8.2 关键优化

1. **动态频道过滤**：
   - 不需要在每个检测点都扫全部20个频道；
   - 已清除的频道不再检测；
   - 已确认全向且定位的频道，不需要在新位置再检测；
   - 只有定向/未知类型的频道才需要多方向检测来确认类型。

2. **粗扫与清除交替**：
   - 不需要等全部13个点扫完再开始清除；
   - 每扫完1-2个检测点，就对已获得≥2次观测的频道进行定位清除；
   - 清除后从新位置继续扫描，减少移动距离。

3. **检测点数量自适应**：
   - 如果在前几个检测点就发现了大部分干扰源并完成清除，可以减少后续检测点；
   - 优先访问能覆盖更多未发现频道的检测点。

4. **定向方向快速估计**：
   - 不需要精确估计定向方向，只要能指导第二个检测点落在覆盖区内即可；
   - 2-3个无信号点即可粗略估计盲区方向。

### 8.3 优化后时间估算

假设12个干扰源（8全向+4定向）：
- 初始扫描（原点+4个方向点，每点扫10-15个频道）：约 5×12×6 = 360秒
- 定位清除（平均每个100秒，含移动+检测+清除）：12×100 = 1200秒
- 补充检测（定向类型识别）：约 4×3×6 = 72秒
- 合计约 1632秒

<span style="color:red">【修正】以上估算均为**虚拟时间**。参照问题3演练实测（虚拟5000～7500秒仅对应现实数十秒），1632虚拟秒远在现实1200秒时限之内，并不偏紧；虚拟时间只需满足360000秒上限，无需刻意压缩，真正需要控制的是现实时间。</span>

<span style="color:red">【补充·实测对照】2026-09-12 真实模拟器演练 4 局：虚拟总时间 6046～7730 秒、现实耗时 8.7～19.0 秒，与上述口径分析一致（详见 §十）。</span>

**实际效率需通过模拟器演练测试验证，并根据结果调整参数。**

## 九、程序实现要点

在问题3程序（`problem3_robot_model1.py`）基础上扩展：<span style="color:red">【改名】原 robot.py，2026-09-12 随统一命名（problemX_robot_modelY）改为 problem3_robot_model1.py；本问实现代码为 problem4_robot_model.py。</span>

1. **新增数据结构**：
   - `signal_patterns[channel]`：列表，记录每个检测点的有信号/无信号模式；
   - `source_types[channel]`：`"omni"` / `"directional"` / `"unknown"`；
   - `directions[channel]`：定向干扰源的估计定向方向（若有）。

2. **新增函数**：
   - `classify_source(channel)`：根据信号模式判断类型；
   - `estimate_direction(channel)`：估计定向方向；
   - `select_second_point_directional(channel)`：考虑定向覆盖范围的第二检测点选择。

3. **修改主循环**：
   - 扫描时记录无信号模式；
   - 定位前先判断类型；
   - 定向干扰源的第二检测点需验证在估计覆盖区内；
   - 增加补充检测阶段。

4. **搜索航点更新**：
   - 使用内外圈错开的13点布局；
   - 支持动态跳过已充分覆盖的航点。

## 十、测试验证

<span style="color:red">【补充】2026-09-12 连接真实模拟器演练 4 局（队号 202610061109），日志存于 robot_model4_logs/ 编号日志 log1～log4（每次运行自动新建编号日志，不覆盖历史）。</span>

<span style="color:red">4 局结果：</span>

| 局次 | 清除/源数 | 无信号 | 未定位 | 清除失败 | 判定定向 | 虚拟时间 | 现实耗时 | 平均每源 |
|------|-----------|--------|--------|----------|----------|----------|----------|----------|
| <span style="color:red">1</span> | <span style="color:red">10/12</span> | <span style="color:red">10</span> | <span style="color:red">0</span> | <span style="color:red">0</span> | <span style="color:red">5</span> | <span style="color:red">7037.1 s</span> | <span style="color:red">19.0 s</span> | <span style="color:red">703.7 s</span> |
| <span style="color:red">2</span> | <span style="color:red">11/12</span> | <span style="color:red">9</span> | <span style="color:red">0</span> | <span style="color:red">0</span> | <span style="color:red">0</span> | <span style="color:red">6046.0 s</span> | <span style="color:red">11.8 s</span> | <span style="color:red">549.6 s</span> |
| <span style="color:red">3</span> | <span style="color:red">11/11</span> | <span style="color:red">9</span> | <span style="color:red">0</span> | <span style="color:red">0</span> | <span style="color:red">3</span> | <span style="color:red">6409.6 s</span> | <span style="color:red">15.1 s</span> | <span style="color:red">582.7 s</span> |
| <span style="color:red">4</span> | <span style="color:red">16/16</span> | <span style="color:red">4</span> | <span style="color:red">0</span> | <span style="color:red">0</span> | <span style="color:red">3</span> | <span style="color:red">7729.7 s</span> | <span style="color:red">8.7 s</span> | <span style="color:red">483.1 s</span> |

<span style="color:red">**平均每源时间**：4 局共清除 48 个源、虚拟合计 27222.4 秒，**总体平均 567.1 秒/源**（与问题3同口径：虚拟总时间/清除数）；按界面真实源数 51 计，**总体平均 533.8 秒/源**（第1局 586.4、第2局 503.8、第3局 582.7、第4局 483.1）。源多的局更低——13 点扫描等固定成本被摊薄。</span>

<span style="color:red">**漏源情况（如实记录）**：4 局界面源数 12/12/11/16，共 51 源；清除 10/11/11/16，共 48；漏 3 个（第1局漏 2、第2局漏 1，第3、4局全清），漏检率约 5.9%。漏源频道均为 0 观测——命中 §1.2 固有局限：边缘位置、扇区朝外的定向源，13 个检测点全部落在其盲区内；对全向源 13 点布局保证 ≤700m 全覆盖，不可能漏。修复方向：增加阶段5 残余搜索——半径 1760m 边界环 12 点（间隔 30°，与 1200m 环错开 15°），仅扫 0 观测频道：朝内扇区由内圈点覆盖、朝外扇区由边界环点覆盖，可近全覆盖。</span>

<span style="color:red">**定向机制验证**：方向引导探针（θ_d）共触发 4 次全部成功（第3局修正频道19：原地无信号→引导探针→重定位清除；第4局补漏频道7、修正频道13/6 同路径）；补漏对称点回退命中 2 次；4 局 0 未定位、0 清除失败（如实标记机制，无谎报）。</span>
