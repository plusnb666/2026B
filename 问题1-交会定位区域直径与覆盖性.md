# 问题1：交会定位法多边形定位区域直径计算与覆盖性分析

## 一、问题分析

已知若干个检测点坐标 $S_i=(x_i,y_i)$ 及某干扰源关于这些检测点的示向度 $\theta_i$，需要：

1. 给出采用交会定位法计算多边形定位区域直径的算法；
2. 判断以定位区域直径为直径的圆能否覆盖此定位区域。

示向度存在 $\pm 1^\circ$ 的测量误差，因此每个检测点对应的不是一条射线，而是一个角度扇区。所有检测点扇区的交集即为干扰源可能存在的定位区域。该区域是一个凸多边形，其直径定义为区域内任意两点之间距离的最大值。

## 二、符号定义

| 符号 | 含义 |
|------|------|
| $n$ | 检测点个数 |
| $S_i=(x_i,y_i)$ | 第 $i$ 个检测点的坐标 |
| $\theta_i$ | 在 $S_i$ 处测得的示向度（度） |
| $\delta$ | 示向度误差半角，$\delta=1^\circ$ |
| $l_i^-$ | $S_i$ 处扇区的下边界射线，方向角 $\theta_i-\delta$ |
| $l_i^+$ | $S_i$ 处扇区的上边界射线，方向角 $\theta_i+\delta$ |
| $W_i$ | 第 $i$ 个检测点对应的角度扇区（楔形区域） |
| $P$ | 定位区域，$P=\bigcap_{i=1}^n W_i$ |
| $D(P)$ | 定位区域 $P$ 的直径 |

## 三、定位区域的数学表示

### 3.1 单个扇区的半平面表示

从点 $S_i=(x_i,y_i)$ 出发、方向角为 $\phi$ 的射线，其方程可表示为：

$$\frac{x-x_i}{\cos\phi}=\frac{y-y_i}{\sin\phi}$$

<span style="color:red">【修正】原式中附加的"射线约束" $x\ge x_i\cos^2\phi+y_i\sin\phi\cos\phi$ 有误，已删除。该约束并不必要：由于 $2\delta=2^\circ<180^\circ$，下文两个半平面不等式的交集自动是以 $S_i$ 为顶点、开口角为 $2\delta$ 的楔形，射线反方向一侧的点已被排除；若需显式写出，正确形式为 $(x-x_i)\cos\phi+(y-y_i)\sin\phi\ge 0$。</span>

扇区 $W_i$ 是方向角介于 $\theta_i-\delta$ 与 $\theta_i+\delta$ 之间的所有射线构成的楔形区域。利用叉积判断点相对于射线的方位：

对于任意点 $X=(x,y)$，定义射线方向向量 $\vec{d}_\phi=(\cos\phi,\sin\phi)$，点相对于射线的位置由叉积判定：

$$\text{cross}(\vec{d}_\phi, \overrightarrow{S_iX}) = \cos\phi\cdot(y-y_i)-\sin\phi\cdot(x-x_i)$$

- 叉积 $>0$：点在射线左侧（逆时针侧）；
- 叉积 $<0$：点在射线右侧（顺时针侧）；
- 叉积 $=0$：点在射线上。

扇区 $W_i$ 内的点同时满足：
- 在下边界 $l_i^-$（方向 $\theta_i-\delta$）的左侧（含边界）；
- 在上边界 $l_i^+$（方向 $\theta_i+\delta$）的右侧（含边界）。

因此，扇区 $W_i$ 可表示为两个半平面的交集：

$$\boxed{
\begin{aligned}
W_i:\quad & \cos(\theta_i-\delta)\cdot(y-y_i)-\sin(\theta_i-\delta)\cdot(x-x_i)\ge 0 \\
& \cos(\theta_i+\delta)\cdot(y-y_i)-\sin(\theta_i+\delta)\cdot(x-x_i)\le 0
\end{aligned}
}$$

整理为标准半平面形式 $a x + b y + c \le 0$：

$$\begin{aligned}
& a_1 x + b_1 y + c_1 \le 0,\quad \text{其中 } a_1=\sin(\theta_i-\delta),\ b_1=-\cos(\theta_i-\delta),\ c_1=-a_1 x_i-b_1 y_i \\
& a_2 x + b_2 y + c_2 \le 0,\quad \text{其中 } a_2=-\sin(\theta_i+\delta),\ b_2=\cos(\theta_i+\delta),\ c_2=-a_2 x_i-b_2 y_i
\end{aligned}$$

### 3.2 定位区域 = 半平面交

定位区域 $P$ 是所有扇区的交集，即 $2n$ 个半平面的交集：

$$P = \bigcap_{i=1}^{n} W_i = \bigcap_{k=1}^{2n} H_k$$

其中 $H_k$ 表示第 $k$ 个半平面。有限个半平面的交集是一个（可能无界的）凸多边形。<span style="color:red">【修正】原判定"当至少有两条示向度射线相交时，定位区域有界"不成立。反例：$S_1=(0,0)$、示向度 $60^\circ$，$S_2=(100,0)$、示向度 $61^\circ$，两条中心线相交于约 $(2506,4340)$，但两个扇区方向区间 $[59^\circ,61^\circ]$ 与 $[60^\circ,62^\circ]$ 存在公共方向 $60.5^\circ$，交集是一条无界长带。定位区域有界的充要条件是所有扇区方向区间无公共方向：

$$\color{red}{\bigcap_{i=1}^{n}[\theta_i-\delta,\ \theta_i+\delta]=\varnothing\quad\Longleftrightarrow\quad P\ \text{有界}}$$

证明：若存在公共方向 $\phi$，则沿 $\phi$ 的射线上、距离各检测点足够远的点，相对每个检测点的方向都任意接近 $\phi$，故落入所有扇区，$P$ 无界；反之若 $P$ 无界，则 $P$ 内存在一条射线 $Q+t\vec{d}$（$t\ge0$），当 $t\to\infty$ 时其相对各检测点 $S_i$ 的方向趋于 $\vec{d}$，由扇区是闭集知 $\vec{d}$ 属于每个扇区的方向区间，与无公共方向矛盾。实际交会定位中，各检测点从不同方位观测同一干扰源，方向区间一般无交集，$P$ 有界。</span>

### 3.3 半平面交算法

采用**增量切割法**计算半平面交：

**输入**：$2n$ 个半平面 $H_k: a_k x+b_k y+c_k\le 0$，以及一个足够大的初始包围盒 $B$。

**输出**：凸多边形顶点列表（按逆时针顺序）。

**步骤**：

1. **初始化**：<span style="color:red">【修正】原算法以充分大的矩形包围盒 $[-3000,3000]^2$ 作为初始多边形。当扇区方向区间存在公共方向时定位区域无界，包围盒会截断真实区域并给出虚假直径；当两条边界线近乎平行时，包围盒也可能截断位于远处的真实交点。现改为以目标圆域 $x^2+y^2\le 1800^2$ 的内接正多边形（如正 96 边形，顶点按逆时针排列）作为初始多边形。题目背景保证干扰源位于该圆域内，而真实干扰源必在所有扇区内（测量误差不超过 $\pm1^\circ$），故圆域裁剪不丢失真实定位区域，且保证结果非空、有界。</span>

2. **逐个切割**：对每个半平面 $H_k$，用其边界直线切割当前多边形：
   - 遍历多边形的每条边 $(p, q)$；
   - 若 $p$ 和 $q$ 都在 $H_k$ 内，则保留 $q$；
   - 若 $p$ 在内、$q$ 在外，则求边与边界直线的交点并保留该交点；
   - 若 $p$ 在外、$q$ 在内，则求交点并保留交点和 $q$；
   - 若都在外，则不保留。
   
   交点计算公式：设 $v_p=a_k p_x+b_k p_y+c_k$，$v_q=a_k q_x+b_k q_y+c_k$，则交点为
   $$p + \frac{v_p}{v_p-v_q}(q-p)$$

<span style="color:red">【补充】数值实现时，顶点"在内"的判断应引入容差 $\varepsilon$（如 $10^{-9}$），以正确处理边界直线恰好通过顶点等退化情形。</span>

3. **结果**：切割完成后得到的多边形即为所有半平面的交集，即定位区域 $P$。若顶点数少于3，则定位区域为空或退化。

**时间复杂度**：$O(n\cdot m)$，其中 $m$ 为多边形顶点数（$m\le 2n$）。<span style="color:red">【补充】整体为 $O(n^2)$；结合 §4 的直径计算，全流程复杂度为 $O(n^2+m^2)$（顶点对枚举）或 $O(n^2+m)$（旋转卡壳——增量切割输出的顶点已按逆时针排序，无需另行求凸包）。</span>

## 四、多边形直径计算算法

### 4.1 基本性质

**定理**：凸多边形的直径一定在两个顶点之间取得。

*证明*：对于凸多边形内任意两点，可将其沿所在直线向边界方向移动，距离单调不减，直至到达顶点。因此最大值必在顶点对之间取得。

### 4.2 暴力枚举法

设多边形有 $m$ 个顶点 $V_0,V_1,\dots,V_{m-1}$，枚举所有顶点对：

$$D(P)=\max_{0\le i<j<m}\|V_i-V_j\|$$

时间复杂度 $O(m^2)$。由于 $m\le 2n$，且实际问题中检测点数 $n$ 通常不大（个位数到十几），暴力法完全适用。

### 4.3 旋转卡壳法（Rotating Calipers）

对于大规模顶点数，可采用旋转卡壳法在 $O(m)$ 时间内计算直径：

1. 取多边形顶点中 $y$ 坐标最小的点（若有多个取最左）作为初始点 $p_i$，取 $y$ 坐标最大的点作为 $p_j$；
2. 计算对踵点对：固定边 $(p_i,p_{i+1})$，找到使该边到点的距离最大的顶点 $p_j$；
3. 依次旋转（递增 $i$），同步更新 $j$，每对相邻边之间检查 $p_i$-$p_j$ 和 $p_{i+1}$-$p_j$ 的距离；
4. 遍历一周后得到最大距离即为直径。

### 4.4 算法伪代码

```
算法：定位区域直径计算
输入：检测点列表 S = [(x1,y1,θ1), ..., (xn,yn,θn)]，误差半角 δ
输出：定位区域直径 D，直径端点对 (V_a, V_b)

1. 半平面集合 H ← ∅
2. 对每个检测点 (xi, yi, θi):
     H ← H ∪ sector_to_halfplanes(xi, yi, θi, δ)
     （每个扇区生成2个半平面）
3. P ← half_plane_intersection(H, 圆域内接正96边形)   【改：原为 bounding_box，理由见 §3.3 步骤1】
     （增量切割法求半平面交）
4. 若 |P| < 3: 返回 "定位区域退化"
5. D ← 0, pair ← (P[0], P[1])
6. 对 i ← 0 到 |P|-1:
     对 j ← i+1 到 |P|-1:
       d ← distance(P[i], P[j])
       若 d > D: D ← d, pair ← (P[i], P[j])
7. 返回 D, pair
```

## 五、覆盖性分析

### 5.1 问题

设定位区域 $P$ 的直径为 $D$，直径端点为 $A$ 和 $B$。以 $AB$ 为直径作圆 $\Gamma$，圆心 $O$ 为 $AB$ 中点，半径 $r=D/2$。问 $\Gamma$ 能否覆盖 $P$？

<span style="color:red">【补充】当达到直径 $D$ 的端点对不唯一时（如正六边形等对称图形），覆盖性结论可能依赖于所选端点对，此时应逐对检验，并在论文中指明所选取的端点对。</span>

### 5.2 凸多边形的覆盖判定

由于 $P$ 是凸多边形，$\Gamma$ 覆盖 $P$ 当且仅当 $P$ 的所有顶点都在 $\Gamma$ 内（含边界）。即：

$$\Gamma \supseteq P \iff \forall V\in \text{Vert}(P),\ \|V-O\|\le \frac{D}{2}$$

因此，覆盖性可通过检查所有顶点到直径中点的距离是否不超过半径来判定。

### 5.3 理论结论：不一定能覆盖

**结论**：以定位区域直径为直径的圆**不一定**能覆盖此定位区域。

**证明（构造反例）**：

考虑三个检测点的交会定位。构造三个角度扇区，使其交集为一个等边三角形 $ABC$，边长为 $a$。

- 等边三角形的直径 $D=a$（任意两顶点距离均为 $a$）；
- 取直径端点为 $B$ 和 $C$，以 $BC$ 为直径的圆 $\Gamma$ 的圆心为 $BC$ 中点 $O$，半径 $r=a/2$；
- 第三个顶点 $A$ 到 $O$ 的距离为等边三角形的高 $h=\frac{\sqrt{3}}{2}a$；
- 由于 $\frac{\sqrt{3}}{2}a \approx 0.866a > 0.5a = \frac{a}{2}$，顶点 $A$ 在圆 $\Gamma$ 外。

因此 $\Gamma$ 不能覆盖等边三角形 $ABC$。

**构造可行性**：上述等边三角形可以作为三个检测点扇区的交集。具体构造方法：

- 边 $BC$ 所在直线作为第一个检测点扇区的下边界射线，检测点位于 $B$ 的外侧延长线上，扇区开口朝向三角形；
- 边 $CA$ 所在直线作为第二个检测点扇区的下边界射线，检测点位于 $C$ 的外侧延长线上；
- 边 $AB$ 所在直线作为第三个检测点扇区的上边界射线，检测点位于 $B$ 的外侧延长线上；
- 每个扇区的另一条边界位于三角形外部，不参与交集边界。

只要检测点距离三角形足够远（如1000米），扇区的另一条边界不会切割三角形，三个扇区的交集就恰好是三角形 $ABC$。

### 5.4 几何解释（泰勒斯定理）

根据泰勒斯定理（Thales' Theorem）：

> 以线段 $BC$ 为直径的圆，点 $A$ 在圆上当且仅当 $\angle BAC=90^\circ$；
> - 若 $\angle BAC>90^\circ$，则 $A$ 在圆内；
> - 若 $\angle BAC<90^\circ$，则 $A$ 在圆外。

对于三角形 $ABC$，若 $BC$ 是最长边（直径），则 $\angle BAC$ 是最大角：
- 若 $\angle BAC\ge 90^\circ$（直角或钝角三角形），则 $A$ 在圆内或圆上，圆能覆盖三角形；
- 若 $\angle BAC<90^\circ$（锐角三角形），则 $A$ 在圆外，圆不能覆盖三角形。

等边三角形是锐角三角形（每个角 $60^\circ<90^\circ$），因此不能被以最长边为直径的圆覆盖。

### 5.5 两检测点情形的补充说明

当仅有两个检测点时，定位区域通常是一个四边形。该四边形的四个顶点分别为：

$$A=l_1^-\cap l_2^+,\quad B=l_1^+\cap l_2^+,\quad C=l_1^+\cap l_2^-,\quad D=l_1^-\cap l_2^-$$

其中 $l_i^\pm$ 分别为第 $i$ 个检测点扇区的上下边界射线。

在典型配置下（两条示向度中心线相交），该四边形的对角线 $AC$（交叉交点连线）为最长对角线，且 $B$、$D$ 两点处的内角为钝角。由泰勒斯定理，钝角顶点在以对边为直径的圆内，因此以 $AC$ 为直径的圆可以覆盖该四边形。

但这一结论仅适用于两检测点的典型情形，不能推广到三个及以上检测点的一般情况。

## 六、数值验证

### 6.1 算例1：两检测点交会定位

- 检测点 $S_1=(0,0)$，示向度 $\theta_1=60^\circ$；
- 检测点 $S_2=(100,0)$，示向度 $\theta_2=120^\circ$。

**定位区域**（4个顶点）：

| 顶点 | 坐标 |
|------|------|
| $V_0$ | $(50.0000,\ 90.2024)$ |
| $V_1$ | $(47.9851,\ 86.5674)$ |
| $V_2$ | $(50.0000,\ 83.2140)$ |
| $V_3$ | $(52.0149,\ 86.5674)$ |

- 直径 $D=6.9884$ 米，端点为 $V_0$ 和 $V_2$；
- 以直径为直径的圆：圆心 $(50, 86.7082)$，半径 $3.4942$；
- 顶点到圆心最大距离 $=3.4942$（等于半径）；
- **覆盖性：能覆盖**。

### 6.2 算例2：三检测点环绕定位

- 干扰源位于 $(500,500)$；
- 检测点分别位于 $(0,0)$、$(1000,0)$、$(500,1000)$，示向度分别为 $45^\circ$、$135^\circ$、$270^\circ$。

**定位区域**（6个顶点，近似正六边形）：

| 顶点 | 坐标 |
|------|------|
| $V_0$ | $(500.0000,\ 482.8444)$ |
| $V_1$ | $(508.8773,\ 491.4171)$ |
| $V_2$ | $(508.5724,\ 508.8882)$ |
| $V_3$ | $(500.0000,\ 517.7652)$ |
| $V_4$ | $(491.4276,\ 508.8882)$ |
| $V_5$ | $(491.1227,\ 491.4171)$ |

- 直径 $D=34.9208$ 米；
- 以直径为直径的圆半径 $17.4604$；
- 顶点到圆心最大距离 $=17.4604$（等于半径）；
- **覆盖性：能覆盖**（正六边形的外接圆即以对顶点连线为直径的圆）。

### 6.3 算例3：反例验证

构造三个检测点，使其扇区交集为边长40米的等边三角形 $ABC$：

- $A=(0,\ 23.0940)$，$B=(-20,\ 0)$，$C=(20,\ 0)$；
- 检测点1：$(-1000,\ 0)$，示向度 $1^\circ$（扇区 $[0^\circ,2^\circ]$，下边界为边 $BC$）；
- 检测点2：$(674.65,\ -755.93)$，示向度 $131.8934^\circ$（扇区 $[130.8934^\circ,132.8934^\circ]$，下边界为边 $CA$）；
- 检测点3：$(-674.65,\ -755.93)$，示向度 $48.1066^\circ$（扇区 $[47.1066^\circ,49.1066^\circ]$，上边界为边 $AB$）。

<span style="color:red">【补充】检测点2、3的坐标 $(674.65,-755.93)$、$(-674.65,-755.93)$ 为四舍五入后的展示值。精确构造为：检测点2位于边 $CA$ 延长线上、距顶点 $C$ 恰 1000 米，检测点3位于边 $AB$ 延长线上、距顶点 $B$ 恰 1000 米，即检测点2 $=C+1000\cdot(\cos 49.1066^\circ,-\sin 49.1066^\circ)$、检测点3 $=B-1000\cdot(\cos 49.1066^\circ,\sin 49.1066^\circ)$，此时扇区边界精确通过三角形顶点，交会区域恰为边长 40 米的等边三角形。若直接以展示值代入复算，直径约为 39.9906 米而非 40.0000 米（偏差约 1 厘米，源于 2 位小数舍入）。配套代码 problem1_algorithm.py 使用精确构造值。</span>

**定位区域**（3个顶点，即等边三角形 $ABC$）：

| 顶点 | 坐标 |
|------|------|
| $V_0$ | $(-20.0000,\ 0.0000)$ |
| $V_1$ | $(20.0000,\ 0.0000)$ |
| $V_2$ | $(0.0000,\ 23.0940)$ |

- 直径 $D=40.0000$ 米，端点为 $B(-20,0)$ 和 $C(20,0)$；
- 以直径为直径的圆：圆心 $(0,0)$，半径 $20.0000$；
- 顶点 $A$ 到圆心距离 $=23.0940 > 20.0000$；
- **覆盖性：不能覆盖** ✓（反例成立）。

## 七、结论

1. **直径计算算法**：将每个检测点的示向度扇区转化为两个半平面，通过半平面交（增量切割法）求得定位区域（凸多边形），再枚举所有顶点对（或旋转卡壳法）计算最大距离，即为定位区域直径。

2. **覆盖性**：以定位区域直径为直径的圆**不一定**能覆盖此定位区域。当定位区域为锐角三角形（如等边三角形）时，第三个顶点在以最长边为直径的圆外，圆无法覆盖。只有当定位区域的所有顶点到直径中点的距离均不超过半径时，圆才能覆盖该凸多边形。

3. **覆盖判定方法**：求出直径端点 $A,B$ 后，以 $AB$ 中点 $O$ 为圆心、$|AB|/2$ 为半径作圆，检查定位区域所有顶点到 $O$ 的距离是否不超过半径。若全部满足则可覆盖，否则不能覆盖。
