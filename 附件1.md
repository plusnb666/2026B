# 附件1：模拟器使用说明

## 1. 机器狗行为规则

### 1.1 初始状态

机器狗成功进入测试后：

- 初始位置为 `(0, 0)`；
- 测向机初始频道为 `1`。

机器狗可以执行开始、结束、移动、切换频道、检测、精确定位和清除等动作。其中：

- 移动没有单独的指令，由 `/measure` 或 `/clear` 中的 `position` 参数体现；
- 切换频道没有单独的指令，由 `/measure` 中的 `channel` 参数体现；
- 光学精确定位已经合并到 `/clear` 指令中。

因此，通信协议中只有四条指令：

- `POST /enter`
- `POST /exit`
- `POST /measure`
- `POST /clear`

表中的耗时是单个动作的耗时，不一定等于完整指令的总耗时。完整虚拟时间计算规则见附件2。

### 1.2 动作与指令对应关系

| 动作 | 是否有独立指令 | 指令 | 说明 | 虚拟世界耗时 |
|---|---|---|---|---:|
| 开始 | 有 | `POST /enter` | 机器狗进入目标区域，虚拟时间和程序运行时间开始计时。指令本身不增加虚拟时间。 | 0 秒 |
| 结束 | 有 | `POST /exit` | 机器狗主动结束测试，成功响应中的退出原因为 `user_exit`。 | 0 秒 |
| 检测 | 有 | `POST /measure` | 输入检测位置和检测频道，模拟器返回检测结果。 | 5 秒 |
| 精确定位并清除 | 有 | `POST /clear` | 输入清除位置和目标频道，返回“未发现”或“已清除”。 | 未发现 3 秒；已清除 5 秒 |
| 测向机切换频道 | 无 | 由 `/measure` 的 `channel` 确定 | 模拟器比较相邻两次合法检测的频道参数，自动判断是否切换。 | 1 秒 |
| 移动 | 无 | 由 `/measure` 或 `/clear` 的 `position` 确定 | 模拟器根据前后位置计算直线距离和移动耗时。 | 距离除以速度 |

## 2. 指令说明与虚拟时间规则

### 2.1 开始：`/enter`

机器狗进入初始位置并开始测试。虚拟世界时钟从此时开始计时，但 `/enter` 本身不消耗虚拟时间。

### 2.2 检测：`/measure`

机器狗移动到指定位置，对指定频道进行检测。一次 `/measure` 的总耗时由三部分组成：

```text
检测总耗时 = 移动耗时 + 切换频道耗时 + 检测耗时
```

具体规则：

- 移动速度为 `5 m/s`；
- 移动耗时等于前后两点的直线距离除以 5；
- 任意两个频道之间切换耗时为 `1 秒`；
- 如果位置没有变化，移动耗时为 0；
- 如果频道没有变化，切换耗时为 0；
- 每次检测动作本身固定耗时 5 秒。

检测结果有三种：

#### `no_signal`

当前位置接收不到指定频道的干扰源信号。可能原因包括：

- 该频道没有未清除干扰源；
- 距离超过有效接收半径；
- 目标是定向干扰源，但当前位置不在其覆盖范围内。

#### `near`

机器狗已经非常接近目标：距离不超过 5 米，并且处于干扰源的有效覆盖范围内。由于信号太强，测向机无法得到示向度。

#### `direction`

当前位置可以接收到指定频道的信号。模拟器会返回 `svd_deg`，即干扰源的示向度。示向度带有一定误差，不应被当作完全准确的真实方向。

### 2.3 精确定位并清除：`/clear`

机器狗提交清除位置和目标频道。只要目标干扰源与清除位置的距离不超过 20 米，就可以精确定位并清除。

`/clear` 的频道参数只表示“想清除哪个频道的干扰源”，不会切换测向机当前频道，也不会产生频道切换耗时。

一次清除操作包括：

1. 启用光学设备进行精确定位，耗时 3 秒；
2. 如果发现目标，启用激光枪清除，额外耗时 2 秒。

返回结果：

- `no_target_in_range`：20 米范围内没有目标，耗时 3 秒；
- `success`：成功清除目标，耗时 5 秒。

同一个干扰源只能清除一次。再次尝试清除已经清除的目标，会返回 `no_target_in_range`。

清除是否成功只与清除位置和目标位置之间的距离有关，与定向干扰源的发射方向无关。

### 2.4 结束：`/exit`

机器狗结束测试。虚拟世界时钟停止计时，`/exit` 本身不消耗虚拟时间。机器狗可以在任意位置结束测试。

### 2.5 运行时间约束

测试流程如下：

1. 用户在模拟器中确认开始；
2. 模拟器准备测试数据；
3. 准备完成后显示 5 秒倒计时；
4. 倒计时结束后开启 25 分钟测试窗口并开放机器狗接口；
5. 机器狗成功调用 `/enter` 后开始计算程序运行时间，最长为 20 分钟。

25 分钟测试窗口和 `/enter` 后 20 分钟程序时间，以较早到达者为准。超时后测试会自动结束并标记为“超时退出”。机器狗在虚拟世界中的活动时长上限为 100 小时。

## 3. 计时示例

假设机器狗进入测试后，先移动到 `(300, 400)`，对频道 1 检测；然后原地对频道 2 检测；接着移动到 `(300, 0)`，尝试清除频道 3，且假设清除失败；最后原地再次检测频道 2。

| 序号 | 指令 | 位置 | 频道 | 移动距离 | 移动耗时 | 切换耗时 | 检测/清除耗时 | 本步总耗时 | 虚拟时刻 |
|---:|---|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | `/enter` | - | - | - | 0 | 0 | 0 | 0 | 0 |
| 2 | `/measure` | `(300, 400)` | 1 | 500 米 | 100 秒 | 0 | 5 秒 | 105 秒 | 105 |
| 3 | `/measure` | `(300, 400)` | 2 | 0 米 | 0 | 1 秒 | 5 秒 | 6 秒 | 111 |
| 4 | `/clear` | `(300, 0)` | 3 | 400 米 | 80 秒 | 0 | 3 秒 | 83 秒 | 194 |
| 5 | `/measure` | `(300, 0)` | 2 | 0 米 | 0 | 0 | 5 秒 | 5 秒 | 199 |
| 6 | `/exit` | - | - | - | 0 | 0 | 0 | 0 | 199 |

第 4 步的频道 3 是清除目标，不会改变测向机频道。因此第 5 步检测频道 2 时，测向机仍然处于频道 2，不需要再次切换。

## 4. 模拟器操作指南

模拟器下载地址：<https://pan.baidu.com/s/1P1yfVjY0RufU93XOdzhOLw?pwd=2026>

提取码：`2026`

技术支持邮箱：`cumcm2026b@163.com`

模拟器是绿色软件，不需要安装，解压后可以直接运行。运行过程中会在所在目录保存必要数据。如果移动模拟器位置，应整体移动模拟器所在目录。

### 4.1 账号与登录

每次运行模拟器都需要联网登录。首次使用时，需要填写竞赛报名系统中的：

- 参赛队号；
- 队员 1 姓名；
- 队员 1 手机号。

密码要求：

- 长度为 12 至 64 个字符；
- 同时包含字母和数字；
- 不能包含完整参赛队号；
- 不能包含手机号中的连续 6 位数字；
- 不能使用常见弱密码。

同一账号不能同时在两台设备上使用。同一设备重新登录会使旧登录失效。设备崩溃或掉电后，服务器最多需要约 120 秒释放旧会话。

### 4.2 联网和截止时间

问题3和问题4的演练测试、正式测试都要求模拟器全程联网。开始测试前，模拟器会检查登录状态和服务器时间。

以下情况不能启动新测试：

- 网络不可用；
- 本机时间与服务器时间相差超过 60 秒。

北京时间 2026 年 9 月 13 日 17:30 之后，不能启动新的演练测试或正式测试。建议在 15:30 之前完成正式测试。

### 4.3 四个测试模块

模拟器提供：

- 问题3演练测试；
- 问题4演练测试；
- 问题3正式测试；
- 问题4正式测试。

演练测试不限次数。问题3和问题4的正式测试各有 3 次机会。正式测试启动成功后，会占用对应的一次机会。

### 4.4 机器狗接口

默认接口地址为：

```text
http://127.0.0.1:2026
```

模拟器空闲时可以修改端口。5 秒倒计时期间、非测试期间和测试结束后，机器狗接口未开放，连接失败属于正常现象。

机器狗程序必须：

- 使用当前登录参赛队号作为 `robot_id`；
- 每个新动作使用新的 `request_id`；
- 逐次发送动作并等待响应；
- 不得并发发送不同动作；
- 网络中断重试同一动作时，复用原请求内容和原 `request_id`。

### 4.5 测试过程和结束

测试页面可以显示机器狗请求和模拟器反馈，默认显示最新 1000 条，可调整为 100 至 5000 条。显示数量不限制实际请求数量，完整行为仍会写入日志。

以下情况会结束测试并关闭接口：

- 机器狗调用 `/exit`；
- 手工中止；
- 25 分钟窗口超时；
- `/enter` 后程序运行超时；
- 虚拟时间超时；
- 技术异常。

“中止测试”需要两次确认。正式测试中手工中止仍会占用一次正式测试机会。

### 4.6 测试案例编码和行为日志

每局测试都有测试案例编码，用于填写结果表和关联日志。

演练测试结束后可以查看：

- 干扰源总数；
- 全向干扰源数量；
- 定向干扰源数量。

正式测试不会显示这些真实数据。

正式测试结束后，模拟器会自动生成加密行为日志并加入上传队列。选手需要从模拟器导出正式日志，放入支撑材料中。日志文件大小必须不超过 2 MB。机器狗程序还应自行记录指令序列和响应信息。
